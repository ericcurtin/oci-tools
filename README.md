# oci-tools

A pure-Rust, monorepo reimplementation of the container and bootable-container
stack — with a bootc-like OS updater that has **no dependency on ostree or
composefs** (deployments are flattened erofs images sealed with fsverity).

| binary         | role                                                        | equivalent |
|----------------|-------------------------------------------------------------|------------|
| `ocirun`       | OCI runtime (runc-CLI-compatible)                           | crun       |
| `ociman`       | daemonless container engine incl. `build`                   | podman     |
| `ocicri`       | Kubernetes CRI server                                       | cri-o      |
| `ocibox`       | pet containers with home/user/host integration              | distrobox  |
| `ocivmm`       | pet microVMs booted straight from OCI images (own VMM)      | —          |
| `ociboot`      | bootable-container OS manager (install/upgrade/rollback)    | bootc      |
| `ociboot-init` | tiny initramfs binary that mounts deployments (dracut)      | —          |

Design pillars:

* **One implementation per function.** One registry client, one blob store,
  one tar/whiteout applier, one erofs builder, one mount library, one runtime
  core — all in `crates/`, shared by every binary in `bin/` (which are thin
  frontends and never depend on each other). CI enforces this.
* **Filesystem policy.** erofs for immutable OS/deployment images; ext4/xfs
  for all writable state. btrfs and zfs are forbidden — CI greps for them.
* **ostree's concepts without ostree.** Immutable images, transactional
  deployments, rollback, boot counting, `/etc` three-way merge, persistent
  `/var` — reimplemented directly on OCI + erofs + fsverity + BLS entries.
* **First-class distros:** CentOS Stream 10 and Ubuntu 26.04, as OS image
  bases and as CI targets (x86_64 + aarch64, tested in VMs).

## Status

Early development, milestone 4 of 8 in progress (`ociman build` works
end to end for a real, deliberately narrow subset of Dockerfiles —
`RUN`/`COPY`/`ADD` execute for real and commit real layers (`ADD`'s own
local-archive-auto-extraction matches real docker's documented
behavior; `ADD` from a remote URL is supported too, never
auto-extracted, matching real BuildKit's own `noDecompress` for that
source kind); multi-stage builds
work via both `FROM <earlier-stage>` and `COPY --from=<earlier-stage>`,
and `COPY --from=<external-image>` (a `--from` naming neither) pulls and
extracts that image just for the one `COPY`; `--build-arg` and
`--target` (building only up to a named stage, pruning everything else
— including a stage that would otherwise fail to build at all) both
work; multiple explicit sources and glob patterns in one `COPY`/`ADD`
both work too (each landing under the destination by its own basename; glob
matching verified byte-for-byte against Go's own `path/filepath.Match`,
the exact matcher real BuildKit itself uses); a real local build cache
now skips re-running/re-copying a `RUN`/`COPY`/`ADD` step outright
whenever an earlier build already produced the exact same result
(full-history-prefix matching plus a real content digest for
`COPY`/`ADD`, ported from real buildah's own model — see
`docs/design/0101`; `--no-cache` disables it)); `FROM scratch` is
supported too, producing a real, genuinely empty base image (no
layers, no inherited config beyond the same default `PATH` real
`docker build`/`podman build` both still bake in even here — see
`docs/design/0114`); `HEALTHCHECK` is parsed and stored (matching real
Docker's own wire format byte for byte — actually running one
periodically stays out of scope, see `docs/design/0116`). Milestone 3
also grew
real
`--memory-swap`/
`--cpuset-cpus`/`--cpuset-mems`/`--security-opt seccomp=`/a real
`podman`-default capability set/`--cap-add`/`--cap-drop`/`--privileged`/
`--read-only`/`-e`/`--env`/`--hostname`/`-w`/`--workdir`/`--entrypoint`/
`-v`/`--volume`/
`ocirun features` (real, checked support-surface introspection,
independently verified byte-for-byte identical to real installed
`runc features` for namespaces/capabilities)/`ocirun create`/`run
--pid-file`/`ocirun ps`/`ociman kill`/`ociman wait`/`ociman rename`/`ociman inspect` for containers/`ociman top` beyond its
own original scope.
Milestone 5 also now has real pieces: `oci-erofs` builds
verified-deterministic erofs images via `mkfs.erofs`, seals/verifies
them with real fs-verity ioctls, and has a detached dm-verity fallback
via `veritysetup` for state filesystems without fs-verity support;
`oci-bls` reads/writes the real GRUB environment block
(`saved_entry`/boot-counting), verified byte-for-byte against the real
`grub-editenv`, reads/writes Type #1 BLS entries and scans
`/loader/entries/` as a directory, implements the real spec's own
`+tries_left-tries_done` boot-counting filename convention, and sorts
entries per the real spec's own "Sorting" section (full UAPI.10
version comparison included), all verified against the real
uapi-group specification's own text and worked examples, some
cross-checked against the real `systemd-analyze compare-versions`
tool too; `oci-mount` attaches/detaches real loop devices
(read-only, direct-io), verified
against the real `losetup`; `ociboot` now has its first real
subcommand, `list`, wiring `oci-bls`'s scan/sort primitives into an
actual "show the real boot menu, in the real order" command. See
[docs/design/](docs/design/) for design notes per
milestone.

| milestone | scope | status |
|-----------|-------|--------|
| 1 | workspace skeleton, `oci-cli-common`, 4-VM CI matrix | **done** |
| 2 | `oci-spec-types`/`oci-registry`/`oci-store`; `ociman pull/images/inspect` | **done** (plus `ociman rmi`, including refusing to remove an image a container still depends on unless `--force`, and `ociman tag`, both beyond the original scope; `ociman inspect`/`ociman rmi`/`ociman tag` also resolve by a real or short image ID, not just a tag reference; `ociman login`/`ociman logout` write/remove real, `podman login`-compatible registry credentials; `ociman push` uploads an already-stored image back to its own registry, verified end to end including a full round trip through a real, independent `docker`; `ociman pull`/`ociman push --tls-verify=false` talk to a real local/private plain-HTTP registry — see `docs/design/0102`-`0103`, `0122`-`0124`, `0126`-`0128`; and `oci_registry::pull` now fetches a multi-layer image's own config+layer blobs with real, bounded concurrency (up to 3 simultaneous connections, matching real Docker's own `max-concurrent-downloads` default) rather than one at a time — finishing scope this crate's own module doc comment had listed as "planned" since early on, verified with a real, non-flaky mock-registry timing proof and a real, measured 1.48× end-to-end speedup pulling a genuine 4-layer image from a real local registry (see `docs/design/0256`), and `oci_registry::push` now does the exact mirror on the upload side (up to 5 simultaneous connections, matching real Docker's own `max-concurrent-uploads` default — genuinely different from the pull side's own 3, not a copy-paste oversight), measured 1.81× faster pushing the same genuine 4-layer image to a real local registry (see `docs/design/0257`), and `ociman images -q`/`--quiet` (0265) — a real self-inconsistency in `ociman`'s own CLI found by comparing it against itself (`ociman ps` already had `-q`; `images` didn't), matching real `docker images -q`/`podman images -q` at the same time — one line per image, the exact same 12-hex-char digest the plain table's own `DIGEST` column already shows (see `docs/design/0265`), and `ociman rm --all` (0266) — matching real `podman rm --all` exactly (real `docker rm` has no such flag at all), a direct, low-risk port of this project's own already-proven `ocibox rm --all` clap shape/policy: `--force`'s existing per-container gate is completely unchanged (`--all` alone still leaves a running container untouched), every container is still attempted even if one fails, and an already-empty store is a real, silent no-op (see `docs/design/0266`), and `ociman rm <id1> <id2> ...` (0267) — multiple explicit container IDs in one call, matching real `podman rm id1 id2` exactly, and (checked directly against a real installed `podman`) its own genuinely two-part policy: every given identifier is resolved to a real container before anything is removed at all (one unresolvable name aborts the whole call, removing nothing, rather than partially removing whichever others happened to resolve), but once every name has actually resolved, a different per-container failure (still running, no `--force`) never blocks removing the other, already-resolved targets, matching `--all`'s own continue-past-failure policy — a real, checked-directly nuance, not a guess from the CLI help text alone (see `docs/design/0267`), and `ociman images --filter` (0268) — `dangling=true`/`dangling=false` (real podman's own literal help-text example) and `label=`/`label!=`, reusing the exact same parsing/matching logic `ociman prune --filter` already established (`docs/design/0192`) via two small shared helpers rather than duplicating it, verified as a genuine zero-behavior-change refactor for `prune` itself (all 25 of its own pre-existing tests still pass unmodified); real podman's own broader filter set (`reference`, `before`/`since`, `id`, etc.) remains a further, similarly-scoped candidate (see `docs/design/0268`), and `ociman rmi <ref1> <ref2> ...` (0269) — multiple explicit image references in one call, matching real `podman rmi img1 img2` exactly, with a genuinely *different* multi-target policy than `ociman rm`'s own all-or-nothing preflight (0267): checked directly against a real installed `podman rmi`, one unresolvable reference among otherwise-valid ones never blocks removing the others, each processed fully independently; `--json` keeps its original single-object shape for exactly one reference (unmodified for full backward compatibility) and prints a JSON array only when more than one reference is given (see `docs/design/0269`), and `ociman rmi -i`/`--ignore` (0270) — a reference that doesn't resolve to any real image is a silent no-op instead of a clear error, matching real `podman rmi --ignore` exactly, including its own checked-directly detail that `--force` alone implies it too (a nonexistent reference is a silent no-op under `--force` with no `--ignore` given at all) while never silencing any *other* failure kind (an in-use-by-container refusal is still reported even with `--ignore`) — `--no-prune` found not to actually apply to this project's own content-addressed store at all (no separate per-build-layer "image" the way real podman's own graph-driver storage model has) (see `docs/design/0270`), and `ociman rmi -a`/`--all` (0271) — removes every image in local storage (not just dangling ones), matching real `podman rmi --all` exactly, still refusing one a container depends on unless `--force` too, every other image still attempted even if one fails; found and fixed a real edge case before it could ever ship as a bug: naively re-resolving each already-enumerated record's own reference string (rather than using the record already in hand directly) could have spuriously tripped the by-ID sibling-tag-ambiguity gate in an order-dependent way whenever a single manifest digest has both several real tags and this project's own untagged-sentinel record (`0179`) present at once — fixed by wrapping each snapshotted record directly rather than re-resolving it, verified against that exact scenario by hand and with a new automated test (see `docs/design/0271`), and `ociman untag` (0283) — a genuinely new top-level command found by comparing `ociman --help` directly against a real installed `podman --help`, removing one or more image references without touching any underlying blobs; checked directly (an initial assumption was wrong, corrected by hand): the first `IMAGE` argument only *resolves* the target and is itself left alone unless also listed among further arguments, but a *single*-argument call untags every real reference of that image at once, not just the one given; an explicit reference belonging to a different image is a clear error removing nothing, and there's no sibling-tag-ambiguity/`--force` gate at all unlike `rmi`'s by-ID case, since removing a pointer is never destructive the way removing the image itself is (see `docs/design/0283`)), and `ociman pull`/`run`/`create --platform` (0307), closing a real asymmetry with `ociman build --platform` (0193): `oci_registry`'s own already-tested multi-platform manifest-index selection (`ImageIndex::select`) was fully implemented but completely unreachable from any CLI surface other than `build`, every other caller hardcoding `Platform::host()`; unlike `build --platform` (which genuinely executes `RUN` steps and so must match the host), this is a pure image-*selection* mechanism with no host-match assertion at all, matching real `podman pull --platform`/`docker pull --platform` exactly — verified end to end against a real multi-arch registry image and a real installed `podman pull --platform`, and against a real, fully offline mock registry serving a genuine two-platform index (see `docs/design/0307`), and `ociman images --filter id=<prefix>` (0349), closing part of `0268`'s own "still ahead" (real podman's own broader filter set) — matches an image's own full manifest digest (hex, no `sha256:` prefix) by prefix, matching real `podman images --filter id=` exactly (`~/git/container-libs/common/libimage/filters.go`'s own `filterID`); a real, checked-directly consequence of real podman's own generic per-key AND combinator, verified directly rather than assumed: two different `id=` values together match nothing at all, not their union, since no single image's id can start with two different prefixes at once (see `docs/design/0349`), and `ociman images --filter digest=sha256:<prefix>` (0350) — the same AND-not-OR rule as `id=`, matching real `podman images --filter digest=` exactly (`~/git/container-libs/common/libimage/filters.go`'s own `filterDigest`); this project's own single `manifest_digest` per image is the only real "known digest" to check against, matching real podman's own multi-digest check exactly for the common, single-platform case (this project has no fat-manifest-list storage concept at all — see `docs/design/0350`), and `ociman search --list-tags` for listing every real tag in a repository (`GET /v2/<name>/tags/list`, the real distribution-spec v2 endpoint, with real Link-header pagination) -- deliberately not implementing real podman's own separate, largely-deprecated free-text v1 search API at all yet, a clear error instead of a silently wrong/empty result (see `docs/design/0371`) |
| 3 | `oci-runtime-core` + `ocirun`; `ociman run/exec/ps/logs` rootless | **done** (plus systemd cgroups with automated failed-scope cleanup, all six real lifecycle hooks including `createContainer`/`startContainer`, running for both the combined `run` and the separate `create`/`start`/`delete` lifecycle, seccomp, resource limits, `--security-opt seccomp=`, a real `podman`-default capability set, `--cap-add`/`--cap-drop`, `--privileged`, `--read-only`, `-e`/`--env`, `--hostname`, `-w`/`--workdir`, `--entrypoint`, `-v`/`--volume`, `ocirun features`, `ocirun create/run --pid-file`, `ocirun ps`, `ocirun update`, `ociman kill`, `ociman wait`, `ociman rename`, `ociman inspect` for containers, `ociman top`, `ociman run -d`/`--detach`, a real rootless-overlay-backed rootfs cache shared with `ociman build` (`docs/design/0108`-`0110`, `0112`, `0115`), and `ociman prune`/`ociman prune --all` for reclaiming it (`docs/design/0111`, `0117`), and `ociman run --pull always/missing/never/newer` for controlling exactly when an image gets (re-)pulled, matching real `podman run --pull` exactly including its own real per-subcommand CLI quirks and `newer`'s own real digest-comparison semantics (never a timestamp — see `docs/design/0140`-`0141`), and `ocirun pause`/`ocirun resume` plus `ociman pause`/`ociman unpause` for real cgroup-v2-freezer-backed suspend/resume, verified end to end against a real running container's own `cpu.stat` (see `docs/design/0142`-`0143`), and a real, computed `paused` status now shown by `ocirun state`/`list` and `ociman ps`/`inspect` once frozen, matching real runc's own display-time-only `isPaused()` (see `docs/design/0144`), and `ociman stats --no-stream` for a real, one-shot CPU %/memory usage/pid-count sample straight from a container's own cgroup v2 accounting files, matching real `podman stats --no-stream`'s own single-sample CPU-percent formula exactly (see `docs/design/0145`), and `ociman cp` for copying files/directories between the host and a container's own storage (running or stopped, host<->container and container<->container both, see `docs/design/0146`/`0151` for its own real, checked-directly limits), and a real, synthesized `/etc/hosts` for every container plus `ociman run --add-host` for extra entries, matching real podman's own entry ordering/precedence rules exactly including its own `--network=none` default (this project sets up no container networking of its own yet — see `docs/design/0147`), and `ociman diff` for listing every real path added/changed/deleted relative to a container's own base image, backed by a real, persisted filesystem snapshot rather than a second, independent extraction of the base image (found and fixed a real false-positive bug from exactly that simpler-looking alternative before ever shipping it — see `docs/design/0149`), and `ociman logs -f`/`--follow` for streaming a still-running container's own output live, stopping automatically once the container itself exits rather than requiring an interrupt (see `docs/design/0152`), and `ociman logs --tail N` for showing only the last `N` already-captured lines, combining correctly with `-f` (only the initial catch-up read is trimmed, new output is never trimmed — see `docs/design/0153`), and `ociman start`/`ociman restart` for re-running an already-stopped container's own already-on-disk bundle again, matching real `podman start`/`podman restart` exactly (found and fixed a real, reproducible race in the process — an already-stopped-*looking* container whose own detached keeper process hadn't actually finished its own bookkeeping yet could have that stale, delayed write silently clobber a subsequent restart's own fresh state moments later — see `docs/design/0154`), and `ociman commit` for creating a new image from a container's own changes relative to the image it was created from, reusing the exact same base-snapshot diffing `ociman diff` already established and the exact same layer-commit machinery `ociman build` already shares, verified with a real round trip (a brand-new container run from the committed image reads back an added file's real content and confirms a deleted file is genuinely gone — see `docs/design/0155`), and `ociman commit --pause` (the real podman default) for freezing a still-running container via the same real cgroup v2 freezer `ociman pause` itself uses for the duration of the commit, verified by directly observing the real, transient `cgroup.freeze` state while the commit is still in flight, not just before/after (see `docs/design/0156`), and `ociman create` for pulling/extracting a container exactly like `run` but never launching it, exposing `ocirun`'s own already-existing separate `create`/`start` lifecycle through `ociman` for the first time (a subsequent `ociman start` then runs it for the first time; found and fixed a real, previously-latent `ociman ps` default-filter bug in the process — a merely-created container would have wrongly shown up in a plain `ps` — see `docs/design/0157`), and `ociman create --rm`/full `--rm` support across `ociman start`/`ociman restart` for a container that was never run yet or is being re-run, found and fixed a real, separate, pre-existing bug in the process — `ociman restart` on any `--rm` container previously made the whole container vanish instead of surviving the restart, matching real podman's own identical "restart's own internal stop never triggers auto-removal" behavior now (see `docs/design/0158`), and a real fix for the separate, deeper performance issue found along the way — `ociman restart` making its own container's *next* stop take several real seconds was actually caused by a background D-Bus thread from `restart`'s own internal stop still being alive at the exact moment its own re-launch half forked a new keeper, violating `fork`'s own single-threaded-caller safety requirement (confirmed directly, not just theorized); fixed by deferring that thread-spawning cleanup until after the new keeper's own fork has already happened, dropping the affected test suite's own real run time from 17–27s to 2.7–12.7s (see `docs/design/0159`), and a debug-only single-threaded-at-`fork()` safety net (`oci_runtime_core::process::fork`) guarding against a recurrence of that exact 0159 bug class anywhere else in the codebase, zero cost in release builds (found and fixed a real overhead regression of its own while building this same safety net — reading `/proc/self/status`'s own `Threads:` line once instead of enumerating `/proc/self/task`'s own per-thread directory entries — see `docs/design/0160`), and `ociman version` for a real, structured version report matching real `docker version`/`podman version`'s own "no remote server" shape exactly, deliberately omitting fields (`GoVersion`, `BuiltTime`) with no honest value rather than fabricating one (see `docs/design/0162`), and `ociman info` for a real, much-narrower-than-podman's system-info report (`host`/`store`/`version`), every field an honest, directly-checkable real value (real `uname(2)`/`sysinfo(2)`, the real resolved storage root, real live container/image counts) rather than an invented placeholder for the parts of real `podman info`'s own huge report this project has no daemon/network-stack/pluggable-storage-driver equivalent of at all (see `docs/design/0163`), and `ociman commit --change` for applying Dockerfile-instruction-style config overrides (`CMD`/`ENTRYPOINT`/`ENV`/`EXPOSE`/`LABEL`/`ONBUILD`/`STOPSIGNAL`/`USER`/`VOLUME`/`WORKDIR`, the exact real set real `podman commit --change` itself supports) as part of a commit, reusing the exact same instruction parser and per-instruction application logic `ociman build` already established rather than duplicating either, and deliberately adding no extra history entries of its own (matching real buildah's own `--change` behavior exactly — see `docs/design/0164`), and `ociman save` for writing an already-stored image out as a real, self-contained `oci-archive`-format tar file, verified by hand against a real `podman load` (a round trip: correct tag, correct config, actually ran) and against a real `docker load`'s own expected rejection of that format (real docker only understands `docker-archive`, confirmed by testing a genuine `podman`-produced `oci-archive` the identical way first) — `--format docker-archive` (real podman's own default) is real, separate, deferred follow-up work (see `docs/design/0165`), and `ociman load` for reading such an archive back, a single linear pass needing no temp-directory extraction (unlike real `containers/image`'s own reader) and verifying every blob against its own claimed digest while ingesting it, the same defense a registry pull already applies — verified against a real, independently-produced `podman save --format oci-archive` archive (loaded, correctly listed, and actually run via `ociman run`), not just this project's own round trip (see `docs/design/0166`), and `ociman save --format docker-archive` (real podman/docker's own default format, `manifest.json` plus flat, decompressed config/layer files — the real, load-critical-only subset, no legacy `repositories` file/per-layer chain-ID subdirectories, checked directly: real `docker load` never reads either) — verified against **both** a real `podman load` and a real `docker load` of the same produced archive, each correctly loading and actually running the image (see `docs/design/0167`), and `ociman load` reading `docker-archive` too (auto-detecting the format exactly like both real tools do, no `--format` flag on load), synthesizing a fresh real OCI manifest since `docker-archive` never stores one, cross-checking every layer's own independently-recomputed digest against the config's own `rootfs.diff_ids` rather than trusting it blindly, and tagging under every `RepoTags` entry a real multi-tag archive names — `--format` now defaults back to `docker-archive` again, matching real podman/docker's own default exactly, now that `load` can round-trip it; verified against a real `podman save`'s own output, a real modern `docker save`'s own output (a real hybrid `oci-archive`/`docker-archive` archive that exposed and got a real bug fixed along the way: `docker save` stashes the image's full reference under a separate `io.containerd.image.name` annotation, not the OCI spec's own `ref.name` one, which real podman itself already knows to prefer first — checked directly against real podman's own source, now matched here too), and this project's own default round trip, all three loading correctly and actually running (see `docs/design/0168`), and `ociman export`/`ociman import` for writing a container's own entire current filesystem out as a real, flat tar and creating a brand-new, single-layer image straight back from one, matching real `docker export`/`docker import`/`podman export`/`podman import` exactly — found and fixed two real bugs along the way, both only surfaced by testing against a genuinely *live*, still-running container rather than an already-stopped one: a naive walk fell straight through a running container's own live `/proc`/`/sys` bind mounts (490MB instead of ~4MB), fixed the same way real `tar --one-file-system` already does it (never crossing a mount-point boundary); and real busybox's own ~380 hardlinked applets were each written as a full independent copy instead of a real tar hardlink entry, fixed to match real `tar`/`docker export`'s own output almost exactly (4,408,832 bytes here vs. a real `podman export`'s own 4,410,880, for the identical live container — see `docs/design/0169`), and `ociman update` for changing a running container's real cgroup resource limits in place (`--memory`/`--memory-swap`/`--cpus`/`--pids-limit`/`--cpuset-cpus`/`--cpuset-mems`, the same subset `ociman run` itself already supports), exposing `ocirun update`'s own already-existing cgroup-apply logic through `ociman` for the first time as a direct library call, verified by reading the real cgroup v2 accounting files back directly afterward (`memory.max`/`cpu.max`/`pids.max` all showed exactly the requested limits) — see `docs/design/0171`, and `ociman healthcheck run` for a real, manual, one-shot invocation of a container's own already-stored `HEALTHCHECK` test (not the periodic scheduler, which stays out of scope), execing the test inside the container's own namespaces via the exact same plumbing `ociman exec` already uses and reporting `healthy`/`unhealthy`/`stopped` matching real `podman healthcheck run`'s own identical output/exit-code shape exactly, verified directly against a real, live `podman healthcheck run` — see `docs/design/0172` for its own one real, honestly-flagged gap (the configured `Timeout` isn't enforced yet), and `ociman volume create/ls/inspect/rm/prune` plus real `-v NAME:/path[:ro]` named-volume support in `ociman run`/`create` (previously a clear, named error — `parse_volume`'s own doc comment used to say so directly), matching real `docker volume`/`podman volume`'s own on-disk layout exactly (`<storage-root>/volumes/<name>/_data`, checked directly against a real `podman volume inspect`) and real podman's own volume-name rule (not real moby's slightly different one), auto-creating a named volume on first `-v` reference exactly like real `docker run -v name:/path`/`podman run -v name:/path`, and refusing to remove a volume a real container still depends on (checked directly against that container's own already-persisted bundle mounts, not a separate parallel record) unless `--force` (which never touches the dependent container itself, matching real `podman volume rm --force` exactly) — see `docs/design/0173`, and `ociman commit --squash` for collapsing a container's own entire current filesystem into a single new layer with no base layers referenced at all, matching real `podman commit --squash`/buildah's own squash mechanism exactly (checked directly: one layer, `Parent: ""`, one history entry), reusing `ociman export`'s own already-tested whole-tree tar primitive (`oci_layer::export_tree`) rather than any diff at all — a squashed container needs no recorded base snapshot, unlike a plain commit — verified against a real multi-layer stack (base image plus two separate real commits on top of it) fully flattened down to one layer while every real change made along the way survives, and directly against a real `podman commit --squash` run during design (see `docs/design/0174`), and untagged commits (`IMAGE` now optional, the natural, symmetric companion to `ociman build`'s own identical change) — a direct, mechanical port of the exact same internal sentinel-reference convention, verified end to end (findable by ID, shown as `<none>`, `ociman push`'s own untagged-image guard and a real `save`/`load` round trip both already worked for it with zero further changes needed anywhere — see `docs/design/0180`), and `ociman prune` now reclaiming a dangling (untagged) image by default, not just with `--all` — matching real `docker system prune`/`podman system prune`'s own identical default exactly, checked directly (not assumed) by building a real dangling image with each and confirming it gets removed with no `--all` at all, while a still-tagged one doesn't (see `docs/design/0181`), and `ociman run`/`ociman create` now resolving an image by its own real or short ID too, not just a tag reference — closing a gap 0179/0180/0181 each separately flagged without ever fixing (`ociman inspect`/`rmi`/`tag`/`push`/`save` already supported this, 0122); ID resolution runs *before* ever trying a tag/pull-policy lookup, specifically so a real image ID (which almost always also parses as some nonsense-but-syntactically-valid tag reference) never triggers a real, wasted network pull attempt first — verified directly (`--pull always` against a real ID completes immediately in a fully offline test rather than hanging on a real registry attempt) — see `docs/design/0182`, and `ociman rmi`'s own sibling-list display now showing `<none>` rather than this project's own internal untagged-image sentinel string when removing by ID with an untagged sibling (0179's own last-named display gap, closed — see `docs/design/0185`), and `ociman start -a`/`--attach` for streaming a restarted container's own live output and blocking, this command's own exit code then becoming the container's own real exit code, matching real `docker start -a`/`podman start -a` exactly (checked directly, including that neither real tool ever prints the container id with `-a` — `ociman restart` has no such flag either in this project or in either real tool — see `docs/design/0186`), and `ociman run -i`/`--interactive` for forwarding this process's own real stdin into the container, closing a real, previously-unnoticed bug found by hand: without it (or before this flag existed at all), `ociman run` and `ociman build`'s own `RUN` steps both silently forwarded real host stdin completely unconditionally, with no way to turn it off, unlike real `docker run`/`podman run`/`docker build`/`podman build`, all checked directly (see `docs/design/0187`), and `ociman create --interactive` plus real stdin forwarding across a later `ociman start --attach`/restart (checked directly against real podman: whether a container's stdin is ever forwarded at all is decided once, at creation, never re-decided by a later `start`'s own flags, which is why `ociman start` has no `-i` of its own at all), found and fixed two real bugs along the way — the detached keeper process unconditionally discarded its own stdin before a later `start --attach` could ever forward it regardless of this new setting, and (found via a routine performance re-check, reproduced on unmodified `HEAD` too, so predating this increment) `ociman run -d --rm` racing its own startup confirmation for a container whose command exits almost instantly, wrongly reported as a setup failure roughly 30-50% of the time when hammered in a tight loop against a release build, now fixed by making the detached keeper's own real exit code the deciding signal instead of always discarding it (see `docs/design/0188`), and `ociman wait` now accepting multiple containers (each one's own real exit code printed on its own line, in the order given), plus `--condition`/`--ignore`, matching real `docker wait`/`podman wait` exactly, including their own checked-directly fail-fast behavior (every name resolved up front; one unresolvable name among several aborts the whole command before printing anything for any of them, `--ignore` turning that into a `-1` placeholder instead) and their own real `-1`-not-a-real-exit-code convention for any condition other than `stopped`/`exited` (see `docs/design/0189`), and `ociman run`'s own real `--security-opt no-new-privileges` (bare, or with an explicit `:true`/`:false`/`=true`/`=false`), found and fixed a real, previously-unnoticed bug along the way — every container had been reporting a real `/proc/self/status` `NoNewPrivs: 1` completely unconditionally, regardless of any flag, since `ociman run`'s own synthesized spec never overrode `Spec::example()`'s own stray `no_new_privileges: true` (correct for `ocirun spec`'s own real-runc-compatible template, never checked against real podman's own actual `run`-time default of `false` until now) — this fix, and the new flag, are verified to genuinely take effect whenever no seccomp filter is actually installed (`--privileged`, `seccomp=unconfined`); a real, deeper, honestly-flagged remaining gap when this project's own default seccomp profile is actually active (this project's own default for every other container) is fully researched and documented, deliberately deferred to its own future increment given its real security-sensitivity (see `docs/design/0190`), and that exact deferred gap now closed too — `NoNewPrivs: 0` genuinely matches real `podman run`'s own default even with the default seccomp profile active, by installing the compiled BPF program via the raw `seccomp(2)` syscall directly (rather than the underlying `seccompiler` crate's own convenience wrapper, which unconditionally forced `no_new_privs` on) and reordering the capability-drop/seccomp-application sequence to match real crun's/runc's own identical two-branch strategy exactly, both checked directly against each reference runtime's own source — re-verified by hand that real, kernel-level seccomp enforcement itself is untouched throughout (see `docs/design/0191`), and `ociman prune --filter label=<key>[=<value>]`/`label!=<key>[=<value>]` for only reclaiming an image whose own config actually matches, resolving a real semantic question along the way by direct, repeated, from-a-clean-state testing against the actually-installed `podman` rather than trusting a first read of its own vendored source (which suggested every filter must independently match, contradicted by the installed binary's own real, repeatable behavior: multiple `label=` values are OR'd together, not ANDed) — any other filter key (real docker/podman also support `until=`/`dangling=`/`reference=`/...) is a clear error rather than silently ignored (see `docs/design/0192`), and `ociman prune --filter until=<duration-or-timestamp>`/`dangling=true|false`, unblocked by a real, previously-unnoticed bug this filter's own implementation surfaced and fixed first — `ociman build` never actually updated a built image's own top-level `created` field, so `until=` would otherwise have compared against the wrong, inherited-from-the-base timestamp for the overwhelmingly common case (see `docs/design/0197`) — `until=` matches real docker/podman's own checked-directly rule exactly (an image whose own `created` is missing or strictly after the computed threshold is kept, not removed), and `dangling=<bool>` always overrides whatever `--all`/no-`--all` would otherwise decide on its own, confirmed directly against a real `podman image prune --all --filter dangling=true` (still dangling-only) and `--filter dangling=false` alone with no `--all` at all (removes every unused image regardless of tag) (see `docs/design/0198`), beyond the original scope, and `ociman attach` for attaching to an already-*running* container's own live output from a real, separate invocation than whatever started it, blocking until it exits and propagating its own real exit code — closing a real, previously-acknowledged gap (`cmd_start`'s own doc comment already named it directly), reusing `cmd_start --attach`'s own `attach_and_wait_for_exit` helper verbatim (it already polls the container's own raw on-disk status, not tied to having just launched it in the same process) — deliberately output-only: real stdin forwarding into an already-detached, already-running container would need genuinely new plumbing this project's own current architecture doesn't have (see `docs/design/0230`), and a real fix for a CI-only test flake found by actually checking real GitHub Actions CI results (not just local checks): `ociman commit --pause`'s own busy-poll for the transient `cgroup.freeze` window could starve the very process it was observing on a contended CI host, occasionally missing the window entirely — fixed by widening the window (more real diff-walk work) and yielding CPU between polls instead of busy-spinning; a separate, initial attempt at a stricter read-only-remount `EPERM` check was tried and then retracted after the next real CI run showed it broke two other, pre-existing, legitimate tests relying on this project's own already-established, deliberate tolerance for a real, environment-dependent rootless `CAP_SYS_ADMIN` limitation (`docs/design/0010`) that applies to any bind-mount remount, not just the internal `/sys` case originally suspected — the one test whose own assertion didn't already account for that limitation was fixed instead, checking the real recorded `config.json` mount option rather than a real write attempt (see `docs/design/0232`), and `ocirun events --stats` (0261) — comparing `ocirun --help` directly against a real installed `runc --help` turned up exactly three missing commands (`checkpoint`/`restore`, needing real CRIU integration, deliberately left alone; and `events`), closed here for its narrow, low-risk `--stats` one-shot mode: pure composition of the same already-tested `oci_runtime_core::cgroups` readers `ocirun update`/`ociman stats` already use, every field checked directly against real runc's own actual cgroup-v2 collection code (`cpu.usage.total`, the *raw* (not working-set-adjusted) `memory.usage.usage`, `memory.usage.limit` with the same real `u64::MAX` "no limit" sentinel, `pids.current`) rather than guessed from the JSON struct's own field names — deliberately narrower than real runc's own much larger `Stats` (no cpuset/blkio/hugetlb/Intel RDT/PSI/network-interface fields, none of which this project has a reader for), matching this project's own "honest, smaller report, never fabricated" convention; the periodic (no `--stats`) OOM-notify mode stays a clear, honest "not yet" error (see `docs/design/0261`), and `ociman exec -w`/`--workdir` (0260) — a real, checked-directly drop-in-fidelity bug fixed: `ociman run`/`create` already correctly used `-w`/`--workdir` matching real `podman run -w` exactly, but `ociman exec` instead only accepted `--cwd`, an entirely different flag name than real `podman exec -w`/`--workdir` (confirmed directly against a real installed `podman exec --help`) — any podman muscle-memory user typing `ociman exec -w ...` got a hard clap parse error; renamed outright (no alias kept for `--cwd`, matching real podman's own identical lack of one, and this project's own pre-1.0 lack of a CLI-stability promise yet) (see `docs/design/0260`), and `ociman system df` (0263) — real disk usage across images (deduplicated by manifest digest)/containers/local volumes, matching real podman's own default summary table, composed entirely from already-tested primitives (`ociman prune`'s own in-use-digest set, `ociman volume`'s own in-use check, the same hardlink-aware `dir_size` walk); a real bug caught by a hands-on manual smoke test before any automated test was even written: a container using this project's own rootless-overlay optimization (0108-0110) leaves `rootfs/` genuinely empty on disk once stopped (the real, persisted writable delta lives in a separate `upper/` directory instead), so an initial `bundle/rootfs`-only implementation reported `0B` for every such container regardless of what it had actually written — fixed by checking for `upper/`'s own presence first (see `docs/design/0263`), and `ociman system df -v`/`--verbose` (0285) — a real per-image/per-container/per-volume breakdown replacing the summary table entirely, matching real `podman system df -v`'s own three headed sections exactly in shape, and closing 0263's own deferred precise cross-image "shared vs. unique" size split with a real computed blob-reference-count pass (this project's own content-addressed store makes it exact, not an approximation) rather than the summary's own simpler "wholly unused images only" rule — verified directly with two distinct images (different `Cmd`, so different manifest digests) sharing one real, byte-identical layer, both correctly reporting a non-zero shared size (see `docs/design/0285`), and `ociman ps --filter status=<value>` (0272) — `ociman ps` had no `--filter` at all; closes the cleanest, best-verified subset of real `podman ps --filter`'s own much larger family, checked directly: multiple `status=` values are OR'd together, and giving `--filter status=` at all overrides the default running-only filter entirely (a `--filter status=created` alone, no `-a`, still shows a merely-created container a plain `podman ps` hides) — real podman's own `label=` showed a still-not-fully-understood real multi-value quirk under direct testing, left for its own dedicated future increment rather than guessed at (see `docs/design/0272`), and `ociman ps --filter name=`/`id=` (0273) — resolved the substring-vs-regex-dependency question `0272` deferred without adding a new dependency: both real docker's and podman's own `name=` filter runs an actual (unanchored) regex search, which for any ordinary, non-metacharacter filter value is behaviorally identical to a plain substring search (verified directly), so that's exactly what this implements instead of a new `regex` crate; `id=` matches real podman's own dedicated checked-directly prefix-match rule for a plain hex value (not a substring-anywhere search); different filter *keys* are ANDed together while multiple values for the *same* key are OR'd (both checked directly), and `id=`/`name=` — unlike `status=` — don't override the default running-only/`--all` visibility rule on their own (see `docs/design/0273`), and `ociman run`/`create --label` (0274) — closing a real, more fundamental gap found while investigating `ps --filter label=`: containers here had no labels of their own at all, only images did; `--label KEY=VALUE`/bare `KEY` (repeatable, reusing `ociman build --label`'s own already-established tolerant parser rather than duplicating it) merges with the resolved image's own inherited `Config.Labels`, a same-key `--label` overriding the image's own value, both checked directly against a real installed `podman create`/`inspect`, and shown via `ociman inspect`'s own new `labels` field; also resolved `0272`'s own flagged "still-not-fully-understood" `ps --filter label=` multi-value quirk along the way — redone from a verified-clean slate, it was test contamination, not a real anomaly: real podman's own container-level label filter genuinely ANDs multiple values (a deliberately *different*, separately real function than `ociman prune --filter label=`'s own already-shipped OR semantics, `0192`) — the filter itself now has real container labels to act on (see `docs/design/0274`), and `ociman ps --filter label=`/`label!=` (0275) — closing that exact follow-up, reusing `ociman prune`/`images --filter`'s own already-shared `LabelFilter`/`try_parse_label_filter` parsing but with `.all()` (AND) instead of `.any()` (OR) combination, matching real podman's own genuinely different container-specific `MatchLabelFilters` confirmed in `0274`; like `id=`/`name=` and unlike `status=`, doesn't override the default running-only/`--all` visibility rule on its own (see `docs/design/0275`), and `ocirun exec -g`/`--additional-gids` (0276) — matching real `runc exec -g` exactly (checked directly: `crun exec`/`podman exec` both have no equivalent flag at all, only `-u`/`--user`'s single primary group), a pure CLI-plumbing fix since the underlying primitive (`User::additional_gids`, `identity::apply_supplementary_groups`) already existed and was already wired into `ExecRequest`; appends to (never replaces) the container's own already-declared supplementary groups, matching real runc's own checked-directly `append` semantics, and composes with `--user` (see `docs/design/0276`), and `ocirun kill -a`/`--all` (0277) — matching real `crun kill --all` exactly (checked directly: `runc kill` has no equivalent flag at all), signaling every real pid in the container's own cgroup via the same freeze/sweep/thaw sequence real crun's own `cgroup_killall_path` uses, reusing already-tested primitives (`cgroups::set_frozen`/`all_pids`, already used by `ocirun pause`/`resume`/`ps`) with zero new engineering; the cgroup is always thawed again before returning even if a signal fails partway through, verified directly by reading the real `cgroup.freeze` file back afterward (see `docs/design/0277`), and `ociman run`/`create --group-add` (0278) — matching real `podman run --group-add`'s own checked-directly resolution rule exactly (`vendor/github.com/moby/sys/user/user.go`'s own `GetAdditionalGroups`): a numeric GID used as-is even without a matching `/etc/group` entry, a name resolved against the container's *own* `/etc/group` (a clear error if it doesn't), duplicates collapsing to one real gid each — reuses `user_resolve.rs`'s own already-tested, symlink-escape-safe `/etc/group` reader (the same one `ociman exec --user` relies on) via one new function rather than duplicating it; the special `keep-groups` value (real podman's own "currently only available with the crun OCI runtime" feature) is a clear, honest "not yet supported" error rather than silently ignored, since it needs annotation-driven runtime-level support this project's own `ocirun` has no equivalent mechanism for yet (see `docs/design/0278`), and `ociman ps --filter before=`/`since=` (0280) — compares a container's own creation time against *another* container's, matching real `podman ps --filter before=`/`since=` exactly, including its own real, checked-directly (not merely trusted from source) multi-value rule: multiple values for the same key use the *earliest* of all the given reference containers' own creation times regardless of key, verified directly with three real containers spaced apart; an unresolvable reference is a clear error, and — like `id=`/`name=`/`label=`, unlike `status=` — doesn't override the default running-only/`--all` visibility rule on its own (see `docs/design/0280`), and `ociman ps --filter ancestor=` (0281) — a real, checked-directly image-name/tag substring match (a bare, tagless value also matching a `:latest`-tagged reference); caught a genuine installed-vs-cloned-source version mismatch along the way (the cloned `~/git/podman` is `v5.4.0-rc1`, materially newer than the installed `4.9.3`): the cloned source suggested a short image-ID prefix should also match, but direct testing against the real, installed binary showed only an *exact* full 64-hex ID does — this project's own "checked directly against a real installed X" principle catching what source-reading alone would have gotten wrong; an exact full-digest match and real docker/podman's own broader image-lineage ("or a descendant") semantics both remain deliberately deferred (see `docs/design/0281`), and `ociman ps --filter exited=` (0282) — matches a container with a real, recorded exit code equal to one of the given values (never one that hasn't exited at all), multiple values OR'd together, checked directly against a real installed `podman ps --filter exited=`; reuses the already-recorded `ANNOTATION_EXIT_CODE` `ociman ps`/`inspect` already show, no new storage needed (see `docs/design/0282`), and `ociman stats`'s own default continuous-streaming mode (0284) — closing the one deliberately narrow gap `0145` left open (`--no-stream` was required; the real default *streaming* behavior was a clear "not implemented" error): `--interval`/`-i` (real podman's own `5`-second default) and `--no-reset` now work, clearing the real terminal screen between samples (checked directly against real podman's own `term.IsTerminal`-guarded `ClearScreen`, never corrupting redirected/piped output with control characters) and ending cleanly — a real success, not an error — the moment the target container stops, a pure loop wrapped around the exact same already-tested cgroup readers/table renderer `--no-stream` already used, verified end to end with a real, short-lived container (see `docs/design/0284`), and `ociman run`/`create -u`/`--user` (0286) — the one remaining CLI surface `resolve_user` never had, matching real `docker run -u`/`podman run -u` exactly (a user name or uid, optionally followed by `:` and a group name or gid, overriding the image's own declared `USER`), pure plumbing onto already-tested infrastructure (`user_resolve::resolve`, already shared by `ociman exec --user` and image-`USER` resolution) — found and fixed a real, previously-unnoticed bug along the way: `resolve_user` only ever validated the resolved *uid* against this project's single-entry rootless uid mapping, never the *gid* the identical way, so a resolved `(uid=0, gid=<non-zero>)` (reachable even before this change, via an image declaring `USER root:staff`) sailed straight through and only failed much later as a bare, confusing `setresgid` `EINVAL` deep inside `identity::apply`, with no indication of what was actually wrong — now surfaced as the same clear "cannot map" error the uid case already gives (see `docs/design/0286`), and `ociman container exists`/`ociman image exists`/`ociman volume exists` (0287) — the one basic CRUD verb this project never had for any resource, matching real `podman container/image/volume exists` exactly (silent, exit-code-only: `0` found, `1` not found; real docker has no equivalent of any of the three); the one deliberate exception to this project's own established "keep every container/image verb flat, not nested under `podman container`/`podman image`-style families" choice, since checked directly neither real tool exposes `exists` any other way — reuses existing, already-tested resolution for all three (the same lookups `ociman inspect`/`rmi`/`tag`/`volume inspect` already share), no new logic (see `docs/design/0287`), and `ociman ps --filter until=` (0289) — matches a container created strictly before a given duration-ago or absolute RFC3339 timestamp, matching real `podman ps --filter until=` exactly (checked directly against `~/git/podman/vendor/go.podman.io/common/pkg/filters/filters.go`'s own `ComputeUntilTimestamp`: exactly one value accepted, a strict `Before` comparison); reused `ociman prune --filter until=`'s (`0198`) own threshold computation verbatim, zero new logic (see `docs/design/0289`), and `ociman ps -n`/`--last`, `--no-trunc`, `--noheading` (0290) — three real, checked-directly `podman ps` table-output flags: `-n`/`--last` (a positive value overrides the default running-only visibility rule like `--all` and keeps only the `n` most-recently-created matching containers, still shown oldest-first); `--no-trunc` (shows the real, full command instead of the newly-added real default 17-character-plus-`...` truncation this closes a genuine prior gap by adding — `ociman ps`'s own COMMAND column was previously never truncated at all); `--noheading` (drops the header row) — matching real `~/git/podman/pkg/ps/ps.go`/`cmd/podman/containers/ps.go` exactly (see `docs/design/0290`), and `ocirun run`/`create --preserve-fds N` (0291) — matching real `runc run`/`crun run --preserve-fds` exactly (pass `N` extra fds, starting at fd 3, through to the container's own process untouched; a claim of more than are actually open fails fast with a clear error, matching real runc's own upfront `Faccessat` check); found and fixed a real, more serious, previously-latent gap while implementing it — this project's own container launch sequence had **no default fd-closing step at all** before this, meaning any fd this project's own caller happened to have open beyond stdio leaked straight into *every* container unconditionally, regardless of any flag, unlike both real reference runtimes' own identical "close everything above stdio (+ preserved) by default" behavior; closed via a single `close_range(2)` syscall from a `pre_exec` closure (verified to add no measurable startup-time cost) (see `docs/design/0291`), and `ociman images --filter before=`/`since=`/`after=` (0293) — an image-creation-time comparison, distinct from `ociman ps --filter before=`/`since=`'s own container version (`0280`); real podman's own generic "AND every filter under the same key" combination rule (checked directly, `~/git/container-libs/common/libimage/filters.go`) is mathematically equivalent to comparing against the *earliest* reference for `before=`, the *latest* for `since=`/`after=` — a real, checked-directly *difference* from `ps`'s own version, which (matching a real, separate quirk in podman's own `ps`-side source) uses the earliest for both keys (see `docs/design/0293`), and `ocirun exec --preserve-fds` (0294) — closing `0291`'s own "still ahead": the identical real `runc exec`/`crun exec --preserve-fds` semantics `run`/`create` already got, for an *additional* process joining an already-running container rather than its first; `close_fds_ge_than` (the shared `close_range(2)` syscall wrapper) moved into `oci-runtime-core`'s own `process` module so `launch.rs`/`exec.rs` share one real implementation rather than each getting its own copy — verified to add no measurable cost to `ocirun exec`'s/`ociman exec`'s own hot path (see `docs/design/0294`), and `ociman images --filter reference=`/`reference!=` (0295) — a real shell-glob match (Go's own `path.Match`) against several candidate forms of each image's own reference, plus a real exact-image-identity shortcut, matching real `podman images --filter reference=` exactly (`~/git/container-libs/common/libimage/filters.go`); reused this project's own already Go-toolchain-verified `path/filepath.Match` port instead of writing a second one, moving it out of `oci-dockerfile`-private code (built for `COPY`/`ADD` wildcards) into `oci-spec-types` the moment this genuinely unrelated caller needed it, the same "shared primitive moves to a shared crate" precedent `resolve_by_reference_or_id`/`time` already established — a real, verified-zero-behavior-change move (see `docs/design/0295`), and `ociman run`/`create --dns`/`--dns-search`/`--dns-option` (0298), closing `0297`'s own "still ahead" — reuses the exact same `oci_runtime_core::resolv_conf::write_resolv_conf` `ocicri` already established with zero changes needed to the primitive itself, confirmed by reading real podman's own richer `libnetwork/resolvconf` package directly: for a container with no private network namespace of its own (this project's own architecture, unconditionally), its much richer algorithm degenerates to exactly the same simple "host copy or explicit values, never blended" rule already ported from real cri-o; `ociman build`'s own `RUN` steps also gained an unconditional real `/etc/resolv.conf` (a straight host copy, no `--dns`-equivalent build flag yet) since a real `RUN apt-get update`-style step genuinely needs working DNS to reach a real package registry (see `docs/design/0298`)), and `ociman run`/`create --stop-signal` (0300) — a create-time override for the signal a later `stop`/`restart` sends first, persisted on the container record itself and taking precedence over the resolved image's own declared `STOPSIGNAL` (`0244`), matching real `docker run --stop-signal`/`podman run --stop-signal` exactly; validated eagerly (a typo'd signal name fails the `run`/`create` itself, not just the first later `stop`), and visible via `ociman inspect`'s own new `stop_signal` field (see `docs/design/0300`), and `ociman run`/`create --stop-timeout` (0301) — a create-time default for the grace period a later `stop`/`restart` waits before escalating to `KILL`, matching real `docker run --stop-timeout`/`podman run --stop-timeout` exactly, including a real, checked-directly CLI-level precedence subtlety found while implementing it: real `podman stop`/`restart --time` only ever overrides the persisted per-container value when the flag is genuinely given (`cmd.Flag("time").Changed`), not merely "whatever value clap's own default produces" — `Command::Stop`/`Command::Restart`'s own `time` field changed from a hardcoded `u64` default to `Option<u64>` to make this same distinction reachable at all (see `docs/design/0301`), and `ociman volume export`/`ociman volume import` (0302) — closing `0173`'s own deferred volume-backup gap: a named volume's own content out to (or in from) a real, flat tar, matching real `podman volume export`/`podman volume import` exactly, including their own checked-directly "merge onto existing content, never wipe first" import semantics; pure composition of already-tested primitives (`ociman export`'s own `oci_layer::export_tree`, `ociman import`'s own gzip-or-plain-tar detection plus `oci_layer::apply`), verified end to end including real, bidirectional interoperability against an installed `podman volume export`/`import` (see `docs/design/0302`), and `ociman images --filter containers=true`/`containers=false` (0303), closing part of `0295`'s own "still ahead" — whether any real container currently uses the image, matched by its own underlying identity (manifest digest) rather than one exact tag string, matching real `podman images --filter containers=` exactly including its own checked-directly stricter value rule (only the literal `true`/`false`, no `1`/`0` shorthand; `containers=external` a clear, honest error, this project having no external-container concept at all); found `ociman prune` and `ociman system df` each independently computing the identical "every image digest any container references" scan, factored into one shared `images_in_use_digests` function this filter reuses as a third caller rather than duplicating a third time (see `docs/design/0303`), and `ocirun spec -f`/`--file` (0306) — matching real `crun spec -f`/`--file` exactly (checked directly against an installed `crun 1.14.1`; real `runc spec` has no equivalent at all), including a real, checked-directly quirk ported faithfully: the default `config.json` destination refuses to overwrite an already-existing file, but an explicit `--file` target is silently overwritten instead, matching crun's own `access(where, F_OK)` pre-check running only when no `-f` was given (see `docs/design/0306`), and `ociman healthcheck run` now enforcing `HealthcheckConfig.timeout` (0308), closing `0172`'s own honestly-flagged gap: a genuinely hung test is killed (`SIGKILL`) once the configured timeout elapses and reported `unhealthy`, rather than blocking the command itself forever — the real complexity `0172` deferred this for (killing the process actually running *inside* the container's joined PID namespace, not just the outer wait-relay `exec` forks for that case) resolved by moving the deadline-aware wait into the same forked relay that already knows the real, inner pid to kill; `0` (undeclared) falls back to real podman's own documented `DefaultHealthCheckTimeout` (`30s`) — verified end to end with a real `sleep 30` test given a 2-second `--timeout`, killed and reported `unhealthy` in ~2s, not 30 (see `docs/design/0308`), and `ociman run`/`create --cidfile` (0309), matching real `docker run --cidfile`/`podman run --cidfile` exactly (checked directly against real podman's own `pkg/util.CreateIDFile`): a plain, non-atomic create-or-truncate write of the container's own (one, honest, short) id with no trailing newline — a real, checked-directly asymmetry found in podman itself (a bad cidfile path leaves the container behind on `create` but not on `run`) deliberately not replicated; this project applies its own already-established "log and tolerate, never fatal" convention instead, the same one `ocirun run --pid-file` already uses for the identical class of auxiliary bookkeeping write (see `docs/design/0309`), and `ociman rm --cidfile` (0310), the natural read-side complement to `0309` — matching real `docker rm --cidfile`/`podman rm --cidfile` exactly (checked directly against real podman's own `cmd/podman/containers/rm.go`), including its own real "first line only, trailing content simply ignored" parsing rule and its own real mutual exclusion with `--all`; every cidfile-sourced id merges into the exact same target list an explicit `ID`/`--name` argument already builds, so this project's own existing "resolve everything first" and multi-target removal logic needed zero changes (see `docs/design/0310`), and `ociman rm --ignore` (0311), closing `0310`'s own "still ahead" — matching real `podman rm --ignore` exactly (checked directly against real podman's own source and live behavior): an id that doesn't resolve to any real container at all is silently skipped rather than aborting the whole call, `--force` implies `--ignore` too (the same convention `ociman rmi --force` already established, whose own identical-shape resolve loop this change directly mirrors), and — verified directly, side by side against the real installed binary — `--ignore` does *not* widen to a still-running-without-`--force` refusal, which stays a hard error either way (see `docs/design/0311`), and `ociman kill --all` (0312), closing the simplest slice of `0311`'s own "still ahead" — matching real `podman kill --all` exactly (checked directly against real podman's own source and live behavior): lists every container, not just running ones, and silently skips (no error, nothing printed) any that isn't in a genuinely killable state (real podman's own `Container.Kill` only ever allows `Running`/`Stopping`/`Paused`, rejecting `Creating`/`Created`/`Stopped` with `ErrCtrStateInvalid`, the one error `--all` tolerates) — verified directly that a `create`d-but-never-`start`ed container is left completely untouched, not just an already-exited one; found, along the way, a real, pre-existing, separately-scoped gap this change doesn't introduce or fix: a signal sent to a *paused* container's own frozen cgroup can be left stuck rather than actually taking effect (confirmed directly against real runc's own `signalInit`, which explicitly thaws after `SIGKILL`; this project's own `process::kill` doesn't yet — see `docs/design/0325` for the later-corrected, cgroup-v2-precise account of exactly which signals this affects) — reproduces identically via the pre-existing, entirely unchanged single-target `kill` path too (see `docs/design/0312`), and `ociman stop --all` (0313) — matching real `podman stop --all` exactly, checked directly and empirically against a real installed binary with a real mix of running/paused/never-started containers, and confirmed genuinely *different* from `kill --all`'s own treatment of a paused container: a never-started (`create`d, never `start`ed) container is silently tolerated (`stop`, unlike `kill`, considers `Created` a legitimately stoppable state), but a genuinely paused one is a real, reported error, `--all` or not, matching real podman's own actual refusal there too; closed two real, pre-existing bugs in the shared `stop`/`restart` logic surfaced by deciding this — a never-started container previously hit a confusing "has no recorded pid" error instead of a tolerant no-op, and `stop`/`restart` on a paused container previously hung silently through the entire grace-plus-escalation window before falsely reporting success (a frozen cgroup queues rather than delivers a signal until thawed, `0312`'s own discovered gap) — now a real, immediate (millisecond, not multi-second) error instead; `restart`'s own `--rm`-annotation handling was fixed alongside it so a failed stop during restart no longer silently and permanently drops a container's own auto-remove behavior (see `docs/design/0313`), and `ociman restart --all` (0315), closing the last of `0311`'s own three-part "still ahead" list (`kill`/`stop --all` closed by `0312`/`0313`) — matching real `podman restart --all` exactly, checked directly and empirically against a real installed binary: genuinely simpler than `kill`/`stop --all`, since every container is restarted with no skip category at all (a never-started one is simply started for the first time, an already-stopped one started again), only a genuinely paused one still a real, reported error while every other container is still attempted; found and fixed a real bug before this ever shipped — this project's own debug-only single-threaded-at-`fork()` safety net (`0160`) correctly caught a genuine violation partway through a real, multi-container `--all` run, since `restart`'s own old-scope cleanup thread (deliberately never joined, `0159`, on the sound assumption the whole process was about to exit anyway) could still be alive when the *next* container's own restart tried to fork, fixed by deferring every such cleanup until the entire `--all` sweep (every container's own fork already done) rather than just the one container that used to be the last thing this process ever did (see `docs/design/0315`), and `ociman restart --cidfile` plus real multi-target `ids` support (0316) — reading real podman's own source directly first turned up a correction to `0315`'s own guess: real `restart` has no `--ignore` flag at all (unlike `rm`/`stop`), only `--cidfile` was ever real; implementing it required first widening `id: Option<String>` to `ids: Vec<String>`, a real, previously-unnoticed gap of its own (real podman's `restart` has always accepted multiple explicit targets, independently of `--cidfile`) — matches real podman's own checked-directly two-phase behavior exactly: every given id resolves first, aborting the whole call before restarting anything if any one doesn't, but once every one resolves each is still attempted regardless of an earlier one's own restart failure, reusing `0315`'s own deferred-scope-reset `restart_many` helper for both `--all` and this new multi-target path (see `docs/design/0316`), and `ociman kill` multi-target `ids` support (0317) — closing another slice of `0316`'s own "still ahead": real podman's own `kill [options] CONTAINER [CONTAINER...]` was never matched here before, only a single target plus `--all`; matches real podman's own checked-directly two-phase behavior exactly (every given id resolves first, aborting the whole call before signaling anything if any one doesn't; once every one resolves each is still attempted regardless of an earlier one's own signal-send failure), printing each one's own raw given name on success rather than a resolved id, matching both this command's own pre-existing single-target convention and real podman's own identical `RawInput`-over-`Id` printing rule; needed no fork-safety handling at all (unlike `restart`'s own identical widening in `0316`), since `kill` never forks a process (see `docs/design/0317`), and `ociman stop --cidfile`/`--ignore` plus real multi-target `ids` support (0318) — closing `stop`'s own remaining real gaps against real podman all together, mirroring the exact combination `ociman rm` already had (0310/0311) and the multi-target shape `restart`/`kill` already got (0316/0317); found and corrected a real, previously-incorrect claim in `0311` along the way — real podman's own `rm`/`stop --ignore` both tolerate a missing `--cidfile` itself (`errors.Is(err, os.ErrNotExist)`), not just an unresolvable name, contradicting `0311`'s own claim that `--ignore` never widened to that case at all; `ociman rm --ignore --cidfile <missing>` (previously a hard error) now also matches, succeeding silently; also matched a related, checked-directly CLI-level subtlety — a `--cidfile` that's given but turns out empty-after-`--ignore`-tolerance, with nothing else given, is a silent successful no-op in real podman, not a "no container ID/name given" error, since that validation only ever checks whether `--cidfile` was *given*, never whether it later resolved to anything (see `docs/design/0318`), and `kill` genuinely thawing a still-paused container to actually deliver its signal (0319), closing a real gap `0312` first found and had deliberately deferred — a signal sent to a paused container could be left stuck rather than actually taking effect, so `kill` on a paused container previously reported success while silently leaving it alive and paused forever (see `docs/design/0325` for the corrected, cgroup-v2-precise account: a genuinely fatal signal like `SIGKILL` actually reaches a frozen process immediately, no thaw needed — what stays queued until thaw is a signal the target process has installed its own handler for, exactly the case a real container's own graceful-shutdown handler puts `stop`'s first signal in); checked directly against both reference runtimes' own source rather than assumed: real runc's own `signalInit` only thaws after `SIGKILL` specifically, real crun's own `libcrun_kill_linux` never thaws at all for any signal — neither actually gets this right in general, so this project's own fix (a new, shared `oci_runtime_core::cgroups::kill_thawing_if_paused`, used by both `ocirun kill` and `ociman kill`'s single-target/multi-target/`--all` paths alike) deliberately generalizes to thaw after *any* signal, a genuine improvement over both real tools, not merely matching one of them; verified end to end with a real, still-paused container in both `ocirun`/`ociman` (see `docs/design/0319`), and `ociman pause`/`unpause --all`/`--cidfile` plus real multi-target `ids` support (0320), the last of this project's own container-lifecycle commands still missing the shape `kill`/`stop`/`restart`/`rm` already got (0310-0318) — one shared function parameterized on freeze-vs-thaw rather than two near-duplicate ones, matching real podman's own near-identical `pause.go`/`unpause.go`; found and fixed a real, pre-existing correctness bug along the way — this project's own `pause`/`unpause` only ever checked `effective_status() == Running` (which a genuinely paused container also reports, since `Paused` is deliberately never persisted), so a double-`pause`/double-`unpause` silently "succeeded" instead of the real, immediate error real podman gives in both cases (verified live, exit 125); fixed by switching to the same real, cgroup-freezer-derived `display_status` `ps`/`inspect` already use, and cross-checked byte-for-byte against a real installed `podman pause --all`'s own identical skip behavior for an already-paused/never-started container in the same mix (see `docs/design/0320`), and `ociman stop`/`restart` genuinely succeeding against a paused container (0324) — closing the single most-repeated "still ahead" item across six consecutive design notes (0313, 0315-0320); a real improvement beyond real podman itself, which also just refuses a paused container outright (`libpod/container_internal.go`'s own `stopInternal` deliberately excludes `Paused` from its own allowed-to-attempt state set, checked directly and confirmed live) — `stop`/`restart` (which shares the identical function unchanged) now thaw the container as part of delivering the first signal, the same real primitive `kill` itself already uses (0319), then proceed exactly as for any other running container, no `unpause` step required first; before this fix, attempting the signal-then-escalate dance against a still-frozen cgroup silently hung for the entire grace-plus-escalation window and then falsely reported success (the same class of bug `0312` first found for `kill`), verified end to end with a real release binary genuinely reaching `stopped` in ~1.9s rather than either erroring or silently doing nothing (see `docs/design/0324`), and `ocirun run`/`create --no-pivot` (0336) — matching real `runc run`/`crun run --no-pivot` exactly: a `chroot`-style root swap instead of `pivot_root(2)`, matching real crun's own simpler `move_root` path including its own real `/sys`/`/proc` unmount-or-hide security step (closing the same real mount-reachable-from-inside-the-jail hazard `pivot_root`'s own relocate-then-unmount already closes), deliberately narrower than real runc's own additional host-mountinfo-scanning hardening this project has no parser for (see `docs/design/0336`), and `ociman inspect --format` (0332) — a Go-template-*lite* `--format`/`-f` flag (dot-path `{{.field}}` lookups only, no pipelines/functions), matching real `podman inspect --format`/`docker inspect --format`'s own commonly-used flag; field names are this project's own JSON output field names directly, not real podman's own Go-struct-field-name casing, and an unresolvable field path is a real, immediate error rather than a silent empty string (see `docs/design/0332`), and `ociman ps --format` (0333) — reusing the exact same Go-template-lite engine `inspect --format` established, one line rendered per listed container, taking priority over `--quiet`/`--json`/the default table when given, matching real `podman ps --format`/`docker ps --format` exactly (see `docs/design/0333`), and `ociman images --format` (0334) — the last natural consumer of that same template engine, reused completely unchanged, one line rendered per listed image, same `--quiet`/`--json`-overriding precedence and real-error-on-an-unknown-field behavior (see `docs/design/0334`), and `ociman volume ls --format` (0335) — a real 4th consumer of the same engine the trio survey itself missed (not `inspect`/`ps`/`images`), matching real `podman volume ls --format`/`docker volume ls --format` exactly (see `docs/design/0335`), and `ociman info --format` (0337) — a 5th consumer of the same engine, reused completely unchanged against `info`'s own nested `host`/`store`/`version` report (`{{.host.hostname}}`, `{{.store.images}}`, ...), matching real `podman info --format`/`docker info --format` exactly (see `docs/design/0337`), and `ociman history --format` (0338) — a 6th consumer of the same engine, one line rendered per history entry newest-first (same order the plain table/`--json` already use), matching real `podman history --format`/`docker history --format` exactly (see `docs/design/0338`), and `ociman stats --format` (0339) — the last consumer of this family, applying to every sample of both `--no-stream`'s single one and the default streaming mode's own repeated ones, matching real `podman stats --format`/`docker stats --format` exactly, including that both also re-render the template each interval rather than just once (see `docs/design/0339`), and `ociman run`/`create`/`exec --env-file` (0341) — reading additional `KEY=value`/bare-`KEY`/`KEY*` entries from a file, matching real `podman run --env-file`/`podman exec --env-file` exactly (checked directly against `~/git/podman/pkg/env/env.go`'s own `ParseFile`/`parseEnv`): repeatable, each file's own entries folding in first (a later file's value overriding an earlier one's for a shared key), `-e`/`--env` always winning last regardless of flag order on the command line; found and closed a related, previously-incomplete gap in `-e`/`--env` itself along the way — a bare `KEY*` (trailing `*`, no `=`) now also sets every currently-set host environment variable whose name starts with `KEY`, matching real podman's own identical wildcard-prefix form, which the real implementation already shares between `-e`/`--env` and `--env-file` alike (see `docs/design/0341`), and `ociman ps -s`/`--size` (0342) — each container's own writable-layer size plus a `(virtual <total>)` figure (image size + writable-layer size), matching real `docker ps -s`/`podman ps --size` exactly, including its own genuinely coarser 3-significant-digit precision (a real, deliberate difference from this project's own `human_size`'s existing 4-digit default already used by `ociman stats`/`system df`); a shared `container_writable_layer_size` helper factored out of `system df`'s own two, previously-duplicated inline copies of the identical computation, verified as a genuine zero-behavior-change move (all 11 of `system df`'s own existing tests pass completely unmodified) — see `docs/design/0342`, and `ociman run`/`create --env-host`/`--unsetenv`/`--unsetenv-all` (0343), matching real `podman run` exactly (checked directly against `~/git/podman/pkg/specgen/generate/container.go`'s own `CompleteSpec`) — including a real, easy-to-get-backwards upstream quirk deliberately ported verbatim rather than "fixed": `--env-host` is applied *after* both `--unsetenv`/`--unsetenv-all`, so it genuinely wins over the image's own declared value for a shared key (not just "fills in what's missing") and genuinely revives a variable either of those just removed, meaning `--unsetenv-all --env-host` together do **not** end up with an empty environment — see `docs/design/0343`, and `ocirun list`'s own `OWNER` column (0345) — a real, previously-flagged-and-forgotten gap from this project's own earliest state-model design note (`docs/design/0004`, milestone 3): every container's own effective-uid-resolved login name (`libc::getpwuid_r(geteuid())`, matching real crun's own identical `get_user_name(geteuid())`, persisted at creation time rather than re-resolved at `list` time the way real runc's own separate `user.LookupId` does), a real, honest empty string on an unresolvable uid rather than a fabricated placeholder, checked directly against a real `whoami` subprocess — see `docs/design/0345`, and `ociman commit --iidfile` (0346), matching real `podman commit --iidfile` exactly — the same bare-`sha256:<hex>`-no-trailing-newline shape `ociman build --iidfile` already established, written after a successful commit (see `docs/design/0346`), and `ociman volume rename` (0347), matching real `podman volume rename` exactly (checked directly, `~/git/podman/libpod/runtime_volume.go`) — a real, atomic directory move (not just a metadata-only rename), refusing a volume any container still depends on (no `--force` escape hatch at all, matching real podman's own unconditional refusal), a new name already resolving to a real, different volume a clear error (`ErrVolumeExists`), and renaming a volume to its own current name a real, silent no-op success — see `docs/design/0347`, and `ociman volume ls -q`/`--quiet` (0348), matching real `podman volume ls -q`/`docker volume ls -q` exactly — names only, one per line, no header; conflicts with `--format`, matching real podman's own identical checked-directly restriction (see `docs/design/0348`), and `ociman run`/`exec --preserve-fds` (0351) — correcting a real, previously-uncaught factual error in this project's own design history (`0294` wrongly claimed real podman lacks this flag on both, citing `0276`, which is actually about an unrelated `ocirun exec` flag and never mentions it at all): checked directly against a real installed `podman run`/`exec --help`, both genuinely have it; `ociman create`, however, genuinely does not, matching real podman's own identical asymmetry there too (`podman create --help` has no such flag) — see `docs/design/0351`, and `ociman inspect -s`/`--size` (0352), matching real `podman inspect -s`/`--size` exactly — reuses `ociman ps --size`'s own already-established `compute_container_size`/`ContainerSizeView` verbatim (`0342`), opt-in the same way; a real, immediate error for an image, matching real podman's own identical checked-directly `"size is not supported for type"` refusal (see `docs/design/0352`), and `ocirun update`'s own ad-hoc `--memory`/`--memory-swap`/`--pids-limit`/`--cpuset-cpus`/`--cpuset-mems` flags (0353), closing a real, previously-documented-but-never-revisited milestone-3 scope limit (`docs/design/0099`) — the exact same set real `crun update` also supports; `--resources`, when given, still silently ignores every ad-hoc flag outright, matching real runc's own identical, checked-directly documented precedence exactly (see `docs/design/0353`), and `ociman kill --cidfile` (0354) — the one sibling of `stop`/`rm`/`restart`/`pause`/`unpause --cidfile` (`0310`/`0316`/`0318`/`0320`) that had genuinely never gotten it, matching real `podman kill --cidfile` exactly (see `docs/design/0354`), and `ociman rm -t`/`--time` (0355), requiring `--force` too, matching real `podman rm -t`/`--time` exactly (`~/git/podman/cmd/podman/containers/rm.go`) — composes the already-tested [`stop_container`] escalation `ociman stop`/`restart` already use rather than a second implementation of the same graceful-then-`KILL` sequence; a deliberate, verified-directly divergence from real podman's own default: `rm --force` *alone* (no `--time` given) stays this project's own already-established fast, immediate `KILL` with no grace period at all, rather than adopting real podman's own slower default (a real grace period even under a bare `--force`) — matching this project's own explicit "beat on destroy time" goal over faithfully reproducing a slower upstream default (see `docs/design/0355`), and `ocirun update --cpu-share`/`--cpu-period`/`--cpu-quota`/`--cpu-burst`/`--cpu-rt-period`/`--cpu-rt-runtime` (0356), extending `0353`'s own first slice with every remaining ad-hoc flag that maps directly onto an already-wired `LinuxCpu` field — the last two genuinely write nothing to any real cgroup file at all on this project's own cgroup-v2-only host, matching real runc's/crun's own identical "accepted on parse, never acted on" status there too, verified directly rather than assumed (see `docs/design/0356`), and `ociman container prune` for removing every real, non-running (`Created`/`Stopped`) container in one call, matching real `podman container prune`'s own identical eligibility filter exactly, checked directly against `~/git/podman/libpod/runtime_ctr.go`'s own `PruneContainers` (see `docs/design/0357`), and `ociman prune`/`ociman prune --all` now also reclaim every real, non-running (`Created`/`Stopped`) container unconditionally, matching a real installed `podman system prune -f`'s own checked-directly default exactly (a stopped container is removed with no `--all` needed at all, and doing so *before* the image pass now correctly frees an image only that container used too — see `docs/design/0358`), and `ociman image prune` for real `podman image prune`'s own narrower equivalent -- the same image/blob/rootfs-cache reclaim passes as `ociman prune`, but never touching any container at all, matching a real installed `podman image prune`'s own checked-directly scope exactly (see `docs/design/0359`), and `ociman volume mount`/`volume unmount` for a named volume's own real, already-accessible host directory path -- unlike real *rootless* `podman volume mount` (checked directly: it refuses outright, needing `podman unshare` first), never refusing at all, since this project's own volumes are always a plain, directly-accessible host directory to begin with, the same real case even a rootFUL `podman volume mount`'s own genuine no-op already covers (see `docs/design/0361`), and `ociman mount`/`unmount` for a container's own real, already-directly-accessible root filesystem path -- printing `PersistedState::rootfs` directly with no separate mount step of its own to perform at all, the same "already there" reasoning `volume mount` (0361) already established, and sharing `cp`/`diff`/`export`/`commit`'s own rootless-overlay-rootfs gap for `mount` specifically (see `docs/design/0362`), and `ocirun exec --cap`/`-c` (additive, matching real `runc exec --cap` exactly -- a real, checked-directly *divergence* from real `crun exec --cap`, which instead replaces the process's entire capability set) plus `--ignore-paused`, which surfaced and fixed a real, previously-existing gap: plain `effective_status()` could never report `Paused` at all, so `exec` had no way to actually refuse a genuinely frozen container in the first place until now (see `docs/design/0363`), and `ocirun update --blkio-weight`, closing the last remaining ad-hoc-flags survey candidate flagged back in `0356`: a new `LinuxBlockIo` spec field plus real cgroup v2 `io.bfq.weight`/`io.weight` fallback logic (tries the raw, unconverted value against `io.bfq.weight` first, since whether the BFQ IO scheduler is actually active can only be known by trying; falls back to `io.weight` with the real, documented linear `[10-1000]` -> `[1-10000]` conversion only on a genuine `ENOENT`), matching real `runc`/`crun update --blkio-weight` exactly (see `docs/design/0366`), and `ociman system reset` for wiping this project's own real storage back to a pristine, empty state in one call, matching real `podman system reset`'s own core force-remove-everything effect exactly -- but deliberately scoped to only what `ociman` itself owns under the shared storage root (never `ocibox`'s own `boxes/` or `ocicri`'s own `cri-containers/`/`cri-sandboxes/`/`cri-bundles/`), unlike real podman's own literal single-tool `graphRoot`/`runRoot` deletion (see `docs/design/0367`), and `ociman diff --format` -- matching real `podman diff --format`'s own checked-directly narrower restriction exactly (unlike `ps`/`images`/`inspect --format`'s own rich Go-template-lite engine, real podman itself only ever accepts the literal `json` here too, erroring clearly for anything else, its own exact message reused verbatim), winning outright over a conflicting global `--json` flag when given (see `docs/design/0368`), and `ociman inspect`'s own new `mounts` field for every real bind mount/named volume a container declares beyond the fixed default set (matching real `podman inspect`'s own `Mounts` field in spirit, deliberately narrower in shape) -- correctly excluding this project's own rootless-overlay-rootfs optimization's internal `destination: "/"` mount entry, a real, internal storage-layer detail no real tool's own inspect output would ever surface either (see `docs/design/0369`), and `ociman inspect`'s own new `started_at`/`finished_at` fields, matching real `podman inspect`'s own `StartedTime`/`FinishedTime` in spirit: overwritten on every real start/reap (checked directly, real podman's own identical unconditional overwrite, not just set once at the very first start), both `None` for a container that has never actually started/stopped at all (see `docs/design/0370`), and `ocirun run` now gives its own container a real, tracked state record for its entire lifetime, matching real `runc run`'s own checked-directly behavior exactly -- a concurrent `ocirun state`/`list`/`exec`/`kill` against the same id, issued from another invocation while the original `ocirun run` is still blocked in the foreground, now sees something real (the same real gap `ociman run`'s own `record_running` already closed for itself, 0023), and the state is fully removed again once the container exits, matching real runc's own checked-directly default (see `docs/design/0373`), and `ocirun run --keep` for leaving that same state behind after exit instead, matching real `runc run --keep`/`crun run --keep` exactly (`shouldDestroy: !cmd.Bool("keep")` in runc, `LIBCRUN_RUN_OPTIONS_KEEP` in crun, both checked directly) -- a later `ocirun delete` is still needed to actually clean it up, matching real runc's own identical two-step expectation (see `docs/design/0374`), and `ocirun run --detach`/`-d` for returning almost immediately instead of blocking in the foreground -- a real, forked keeper process runs the container (mirroring `ociman run -d`'s own already-shipped pattern, `0098`), and this invocation prints nothing at all on success, matching real `runc run -d`'s own checked-directly silence rather than `ociman run -d`'s own id-printing convention (see `docs/design/0375`), and `ociman run`/`ociman create --ulimit NAME=soft[:hard]` for setting a container's own `RLIMIT_*` resource limits, matching real `docker run --ulimit`/`podman run --ulimit` exactly (checked directly against `~/git/moby`'s own `go-units.ParseUlimit`/`ulimitNameMapping`) -- including a real, previously-unnoticed rootless divergence found and fixed along the way: an unprivileged process can never actually set an unbounded (`-1`) hard rlimit at all (`EPERM`), so a `-1` on either side is silently clamped to the calling process's own real, current `getrlimit(2)` ceiling instead, matching real podman's own identical `ClampRlimitToHost` (see `docs/design/0376`), and `ociman run`/`ociman create --shm-size` for sizing `/dev/shm`, matching real `docker run --shm-size`/`podman run --shm-size` exactly (same real `go-units.RAMInBytes` byte-count-plus-unit-suffix grammar `--memory` already uses, reused verbatim) -- rewriting the already-present default `/dev/shm` tmpfs mount's own `size=` option in place, in real docker's/podman's own literal plain-byte-count format rather than this project's own pre-existing `size=65536k` shorthand, verified as a real, kernel-enforced tmpfs write limit, not just a persisted-JSON change (see `docs/design/0377`), and `ocirun run`/`ocirun create --no-new-keyring` -- found and fixed a real, previously-unnoticed isolation gap along the way: every container previously silently shared the host's own session keyring with no isolation at all (confirmed by grepping this project's own entire history for `keyctl`/`keyring`: zero hits anywhere); now every `ocirun run`/`ocirun create` (and, transitively, every `ociman run`/`ociman create`, `ocibox enter`, `ocivmm create`, `ociman build`'s own `RUN` steps, and `ocicri` container) joins a fresh, container-scoped session keyring by default, matching real `runc run`/`crun run`'s own unconditional default exactly (checked directly, `~/git/runc/libcontainer/standard_init_linux.go`'s own `keys.JoinSessionKeyring`; `~/git/crun/src/libcrun/linux.c`'s own `syscall_keyctl_join`), verified as a real, kernel-level keyring genuinely named after the container's own id, not a persisted-JSON check (see `docs/design/0378`), and `ociman run`/`ociman create --tmpfs DEST[:OPTIONS]` for mounting an anonymous, kernel-managed tmpfs at an arbitrary container path, matching real `docker run --tmpfs`/`podman run --tmpfs` exactly (checked directly against both real tools' own overlapping-but-not-identical option sets: `size=`/`mode=`/`ro`/`rw`/`exec`/`noexec`/`suid`/`nosuid`/`dev`/`nodev` supported, real docker-only `uid=`/`gid=`/`nr_inodes=`/`nr_blocks=`/`mpol=` and real podman-only `tmpcopyup`/`noswap`/`U`/... rejected with a clear error), always getting both real tools' identical unconditional `noexec,nosuid,nodev,rprivate` defaults -- a destination already used by `-v`/`--volume`, or matching one of this project's own existing default mounts, is a clear, immediate error (a deliberate narrowing of real docker's own "silently replaces the default" behavior), verified end to end as a real, kernel-enforced sized/moded tmpfs and a real `noexec`-vs-`exec` distinction, not just persisted JSON (see `docs/design/0379`), and a fix for a real, previously-stale `ocirun features` claim: its own `hooks` list still reported only four of the six real, already-implemented-and-tested lifecycle hooks (`createContainer`/`startContainer`, genuinely executed since `0088`, were never added to this hand-typed list after that landed) -- now reports all six, in real `runc features`'s own exact order, matching an installed `runc features`'s output byte-for-byte (see `docs/design/0380`), and `ocirun update --cpu-idle` (0382), matching real `runc update --cpu-idle` exactly (real `crun update` has no CLI flag of its own for this either, only via its own `--resources` JSON path, matched here too) -- a new `LinuxCpu.idle` field, written to the real cgroup v2 `cpu.idle` file deliberately *after* `cpu.weight`/`cpu.max` in the same update, matching real crun's own write order exactly rather than real runc's own opposite order (which needs its own separate workaround for the real kernel-enforced `EINVAL` a `cpu.weight` write hits once `cpu.idle=1` is already in effect) -- verified directly against a real kernel: a combined `--cpu-share`/`--cpu-idle` update succeeds cleanly with `cpu.idle` correctly taking final effect, confirmed by the kernel's own silent internal weight reset actually showing up in a re-read `cpu.weight`, not just "no error was returned"; also found and fixed a second, unrelated stale claim in this same doc comment (`--blkio-weight` was still listed as unimplemented long after `0366` actually built it) (see `docs/design/0382`), and `ociman run`/`ociman create --replace` (0384), closing `docs/design/0032`'s own original "what's still not here" gap -- matching real `podman run --replace`/`podman create --replace` exactly (checked directly against source, `~/git/podman/cmd/podman/containers/create.go`'s own `replaceContainer`): force-removes an existing container of the same `--name` first, including one still genuinely *running*, before the normal name-uniqueness check ever runs -- requires `--name` to also be given, matching real podman's own exact error wording verbatim; found and fixed a real fork-safety hazard along the way, the identical class `docs/design/0159` already found and fixed for `ociman restart` (a background D-Bus thread from the replaced container's own best-effort failed-scope cleanup, still alive at the moment of the brand new container's own subsequent `fork()`), fixed the same way: deferring that cleanup until after the new container's own fork has already happened (see `docs/design/0384`), and `ociman exec -i`/`--interactive` (0385), fixing a real, previously-unnoticed correctness bug: before this flag existed, `ociman exec` always forwarded whatever stdin its own caller had, unconditionally -- unlike real `podman exec`'s own checked-directly default (`-i` absent) of never connecting the exec'd process's stdin at all (`~/git/podman/cmd/podman/containers/exec.go`) -- `stdout`/`stderr` remain unconditionally forwarded either way, matching that same real asymmetry; the shared `oci_runtime_core::exec` primitive gained a `close_stdin` field mirroring `crate::launch`'s own already-proven mechanism exactly (a pre-opened, host-side `/dev/null` reconstructed via `Stdio::from_raw_fd`, not a `pre_exec`/`dup2` trick), with `ocirun exec` (which, checked directly against real `runc exec`/`crun exec`, has no such flag or concept at all) and `ocicri`'s own `ExecSync` helper both explicitly kept at their existing, correct behavior (see `docs/design/0385`), and `ociman ps --sort` (0386), matching real `podman ps --sort` exactly (all eight real keys -- `command`/`created`/`id`/`image`/`names`/`runningfor`/`size`/`status` -- checked directly against source and a real installed `podman ps --sort`, not assumed): applied after `--all`/`--filter`/`--last` have already selected which containers to show, matching real podman's own checked-directly "selection stays creation-time-based regardless of --sort" behavior; `--sort runningfor` genuinely sorts by when each container was actually *started* (`PersistedState::started_at`, a new internal-only `ContainerView` field), not an alias for `--sort created` -- verified directly with a container created second but started first, correctly sorting before one created first but started second (see `docs/design/0386`), and `ocirun exec --pid-file` for writing the exec'd process's own real, host-visible pid to a file, matching real `runc exec --pid-file`/`crun exec --pid-file` exactly -- including correctly reporting the *inner* relay fork's pid (never the outer one) when a PID namespace is joined, matching real runc's own `setnsProcess.execSetns` behavior (see `docs/design/0387`), and a real, previously-silent `umask(2)` gap fixed for every container this project ever launches (`ocirun run`/`create`, `ociman run`/`create`, `ocibox enter`, `ocicri`-managed containers): none of them ever called `umask(2)` at all, so a container's own file-creation permissions used to depend entirely on whatever umask the *launching* process/shell/systemd-unit happened to have, rather than the deterministic `0o022` default every real `runc`/`crun` themselves always apply -- `process.user.umask` (a real runtime-spec field this project's own types previously had no room for at all, silently dropping it even from an already-captured real `podman`-generated `config.json` fixture) now round-trips and is applied unconditionally in the shared `identity::apply`, plus a new `ociman run`/`create --umask` matching real `podman run --umask` exactly, verified end to end with a real busybox `umask` call inside a running container (see `docs/design/0393`), and `process.oomScoreAdj` + `ociman run/create --oom-score-adj` (`docs/design/0394`) -- a real, previously-silent gap of the same shape: this project never called `/proc/self/oom_score_adj` at all before now (matching real crun's own identical container-creation-time-only scope, checked directly -- never applied for an exec'd process either, by either real tool), and the underlying spec field had nowhere to round-trip to; verified end to end with a real, positive value written into and read back from a running container's own `/proc/self/oom_score_adj`, and `linux.sysctl` + `ociman run/create --sysctl` (`docs/design/0395`) -- a real, previously-silent gap of the same shape: this project's own types had no room for a real config.json's own `linux.sysctl` map at all, and no code ever applied one; ported real crun's own `validate_sysctl` allow-list exactly, including its real safety property that a `net.*` key can never succeed for any container this project itself launches (rootless containers never have a real, private network namespace of their own) rather than silently leaking a write into the *host's* own real networking configuration -- verified end to end with both a real, honored `kernel.*` sysctl inside a running container and a real, immediate rejection of a `net.*` one, and `linux.resources.unified` + `ociman run/create --cgroup-conf KEY=VALUE` (`docs/design/0398`) -- a real, raw cgroup v2 interface-file passthrough matching real `podman run --cgroup-conf` exactly, applied with higher precedence than every other resource flag (`--memory`/`--cpus`/...), matching real crun's own checked-directly `write_unified_resources` ordering; found and fixed a real, previously-latent bug along the way while verifying this end to end -- `systemd_cgroup::create_scope`'s own returned cgroup path was actually relative to the cgroup v2 mount point despite its own doc comment's claim otherwise (every prior caller had discarded the `Ok` value outright, so nothing had ever dereferenced it as a real filesystem path before), now genuinely absolute -- verified end to end with a real live `memory.max` cgroup file, including a real precedence proof that `--cgroup-conf` wins over an overlapping `--memory` value, and `ociman run`/`create`/`update` and `ocirun update --memory-reservation` (`docs/design/0401`) -- a real, soft memory limit distinct from `--memory`'s own hard cap, matching real `docker run --memory-reservation`/`podman run --memory-reservation`/`runc update --memory-reservation`/`crun update --memory-reservation` exactly; the underlying spec field and both cgroup drivers (raw cgroupfs `memory.low`, systemd's own `MemoryLow` D-Bus property) were already fully wired from an earlier increment, so this was pure CLI plumbing -- verified end to end against the real, live `memory.low` cgroup file/systemd property, including a bare `--memory-reservation` with no `--memory` at all and `ociman run/create --label-file` (`docs/design/0402`), matching real `podman run --label-file`/`podman create --label-file` exactly -- the one remaining sibling of `--label` (`0274`), reusing the exact same shared file format `--env-file` (`0341`) already established (confirmed real podman itself shares one real parsing function, `parseEnvOrLabelFile`, between the two), folding each file's own entries in before `--label` itself, which always wins for a shared key regardless of flag order -- verified end to end via `ociman inspect`'s own `labels` field and `ociman ps --filter command=<substring>` (`docs/design/0404`), matching real `podman ps --filter command=` exactly -- a real regex match against just the container's own first command element (the executable itself, never its arguments), simplified to the same substring match `name=` already established but applied to the correct, first-element-only scope; verified end to end that a container's own real argument (e.g. `sleep`) never itself matches, only its own actual executable does, the real distinction a naive whole-command-line substring match would have silently missed |
| 4 | `oci-dockerfile`; `ociman build` (multi-stage, cache) | in progress — `RUN`/`COPY`/`ADD`/`--build-arg`/`--target` work end to end and commit real layers, including multiple explicit sources, glob patterns, `COPY --from=<external-image>`, `ADD` from a remote URL, multiple `ARG` names on one line, `COPY`/`ADD --chmod`/`--chown`, a real local build cache (`--no-cache` to disable), `FROM scratch`, `HEALTHCHECK` (parsed/stored, not executed), `ONBUILD` (real cross-build execution, not just parsed/stored), a declared `ARG`'s own value now visible in a later `RUN` step's own shell environment (matching real `docker build`/`podman build` exactly — see `docs/design/0119`), a build's own scratch rootfs reclaimed by `ociman prune` instead of deleted synchronously on every build (a real ~26% speedup, now measurably *faster* than `podman build` rather than slower — see `docs/design/0121`), `ociman build --tls-verify=false` for a `FROM`/`COPY --from=<external-image>` pulled from a real local/private plain-HTTP registry (see `docs/design/0129`), real `.dockerignore`/`.containerignore` support for `COPY`/`ADD` (gitignore-style patterns including `**`/negation, `.containerignore` preferred outright over `.dockerignore` when both exist, plus `--ignorefile` to read an arbitrary path directly, verified directly against real `podman build`, deliberately never applied to `COPY --from=<stage>`/`--from=<external-image>`, and — after a routine performance re-verification exposed the build cache's own content-hashing step still reading every byte under an excluded directory unconditionally — a real fix so an excluded directory costs virtually nothing at all rather than merely not being copied — see `docs/design/0130`-`0133`), `ociman build --iidfile` for writing the built image's own digest to a file (see `docs/design/0134`), `ociman build --label` for setting image metadata from the command line, applied after (and overriding a same-key) `LABEL` in the Containerfile itself, matching real `podman build --label` exactly (see `docs/design/0135`), `ociman build --annotation` for setting the built manifest's own top-level OCI annotations (distinct from `--label`'s `Config.Labels`, confirmed against a real pushed manifest's own raw JSON — see `docs/design/0136`), `ociman build --pull always/missing/never/newer` for both `FROM` and `COPY --from=<external-image>`, matching real `podman build --pull` exactly including its own real bare-flag-defaults-to-`always` quirk (see `docs/design/0140`-`0141`), and `ociman history` for viewing a built image's own real layer history (see `docs/design/0050`-`0060`, `0068`, `0072`-`0079`, `0097`, `0101`, `0104`, `0114`, `0116`, `0118`-`0119`, `0121`, `0129`-`0136`, `0140`-`0141`), and `ociman build --add-host` for a real, extra `/etc/hosts` entry visible to every `RUN` step, verified to never actually land in any committed layer (see `docs/design/0148`), and a real, working `SHELL` instruction — a later shell-form `RUN` in the same stage is genuinely invoked through whichever shell `SHELL` most recently declared (previously a documented no-op), scoped to that one stage only and reset to the real default at every new `FROM`, matching real `podman build`'s own actual, checked-directly behavior exactly — including one real narrowing versus Docker's own documentation: `SHELL` never affects `CMD`/`ENTRYPOINT`'s own shell-form wrapping either, confirmed directly against a real `podman build` (see `docs/design/0175`), and `ociman build --squash` for folding every layer the build's own target stage adds into one new layer on top of the base (left completely untouched, unlike `ociman commit --squash`'s own whole-rootfs flattening), matching real `podman build --squash` exactly — checked directly, including one striking byte-for-byte match (an empty squashed layer's own digest, for a metadata-only stage, was identical to real podman's independently-produced one): only the target stage is affected (an earlier stage feeding it via `COPY --from=` still builds completely normally), the full per-instruction history survives (only the very last entry keeps its own real layer weight, matching `podman history`'s own exact display shape), and the build cache is bypassed entirely for the whole build, matching real podman's own identical behavior (see `docs/design/0177`), and `ADD --checksum=sha256:<hex>` for verifying a remote-URL source's own real content before it's ever written anywhere, matching real BuildKit's/buildah's own checked-directly restrictions exactly (only ever legal for exactly one, remote-URL source; a mismatch is a hard error with nothing written and no layer committed; `sha512`/anything else is a clear, immediate "only sha256 is supported" error, matching real Docker's own public documentation rather than real BuildKit's own slightly more permissive vendored digest parser — see `docs/design/0178`), and `ociman build --squash-all` for folding the base image's own layers in too, not just the target stage's own newly-added ones (unlike `--squash`), leaving exactly one layer total and discarding the base's own inherited history entirely, matching real `podman build --squash-all` exactly — checked directly, including a real, previously-latent bug found and fixed along the way: `clone_cache_tree` (used to populate every build's own scratch rootfs from an external base image) never preserved hardlinks at all, silently exploding a real busybox-style base image's own ~380 hardlinked applets into independent full copies on every single build, squash or not — a real, measured ~465MB→~4.2MB disk-space fix for every build using a hardlink-heavy base image, not just this one (see `docs/design/0184`), and untagged builds (`-t`/`--tag` now optional) — a build with no tag is still fully usable by ID, recorded under a real, collision-free internal sentinel reference rather than requiring `oci_store`'s own schema to change at all, matching real `docker build`/`podman build` with no `-t` exactly, and shown as `<none>` by `ociman images` (never the internal sentinel string); found and fixed two real, latent bugs along the way (unrelated to each other, both predating this increment): `ociman push`'s own destination resolution would have silently misparsed the internal sentinel into a nonsense push target instead of erroring clearly, and `ociman load`'s own already-correct doc comment ("a real, supported case, not an error") for a tagless archive was contradicted by its actual code, which recorded no pointer at all for one, making it invisible to `ociman images`/unresolvable by ID/vulnerable to the very next `ociman prune` sweeping it — both now fixed (see `docs/design/0179`), and `ociman build --platform`/`FROM --platform=`, closing a real, previously-unnoticed silent gap found by re-surveying this milestone's own current source directly rather than continuing to drift into milestone-3-adjacent polish: `FROM --platform=<value>` was already parsed but never read anywhere at all, so a Containerfile requesting a non-host platform silently got the host platform instead with no warning whatsoever — this project has no real cross-architecture emulation, so a platform that doesn't match the host is now a clear, immediate error instead (a matching one, even given explicitly, still builds completely normally), with the same real per-stage-overrides-global precedence real BuildKit itself uses, checked directly against its own source (see `docs/design/0193`), and `ociman build --unsetenv <NAME>` for removing an environment variable from the final built image regardless of whether it came from the base image's own inherited config or a real `ENV` instruction, matching real `docker build --unsetenv`/`podman build --unsetenv` exactly including their own checked-directly "no history entry of its own" behavior (unlike `--label`) — found and fixed a second, real, previously-unnoticed bug while verifying it end to end: unsetting an image's *only* declared environment variable (the first thing that actually makes this reachable in practice) used to leave a stray `TERM=xterm` behind, a leftover from `Spec::example()`'s own placeholder `process.env` never actually cleared, instead of matching real `podman run`'s own identical fallback (a real `PATH`, confirmed directly, but never `TERM`) — this project's own `ocirun` has no equivalent fallback of its own at all, correctly matching real `crun run`/`runc run` exactly (see `docs/design/0194`), and `ociman build --unsetlabel <KEY>` for removing a label the *base image itself* declared — checked directly against real `podman build --unsetlabel`, a genuinely different rule from `--unsetenv`'s own (real podman's own help text hints at this: "unset label **when inheriting** labels from base image"): a label only ever declared by this Containerfile's own `LABEL` instruction is left completely untouched even when named, while a base-inherited key is still removed even if a later `LABEL` in this same Containerfile redeclares it (see `docs/design/0195`), and `ociman build -q`/`--quiet` for suppressing every line of build output except the final image digest — checked directly against a real `podman build -q`: not just the `STEP`-by-`STEP` announcements this project's own non-quiet default never had in the first place, but also a `RUN` step's own live stdout, the `tagged: ...` line, and the unused-`--build-arg` warning, all suppressed, matching real podman's own single-line output exactly; found and fixed a real bug while verifying this end to end — the first attempt dup'd a *read-only* `/dev/null` onto a `RUN` step's own stdout/stderr, making every subsequent `write(2)` fail with `EBADF` and turning an entirely ordinary `RUN echo hi` into a spurious nonzero-exit build failure under `-q` alone (see `docs/design/0196`), and a fix for a real, previously-unnoticed bug found while scoping `ociman prune --filter until=`: the built image's own top-level `created` field was never actually updated anywhere in this file, so it silently kept its base image's own original `created` forever regardless of how many `RUN`/`COPY`/`LABEL` instructions (or `--label` flags) actually ran — checked directly against real `podman build`, `created` always mirrors the built image's own *last* history entry (already correctly, individually timestamped by this project's own `record_layer`/`record_empty_history`, just never propagated to the top level before now), so any instruction or `--label` that adds one bumps it to the real, current build time, while a bare `FROM` with no instructions and no `--label` is a true no-op, left completely unchanged from the base, exactly matching real podman's own identical behavior for that shape too (see `docs/design/0197`), and `ociman build --build-arg-file` for reading additional `--build-arg`-style entries from a file (one per line, blank/`#`-comment lines skipped, every file applied before any explicit `--build-arg`, which still wins for a shared key) — checked directly against a real `podman build --build-arg-file`; investigating `--timestamp` first surfaced a real, deeper reproducibility subtlety worth documenting honestly instead of half-implementing: real `podman build --timestamp` doesn't just rewrite `created`/history metadata, it also normalizes newly-committed layers' own file mtimes, which would need `oci_layer::export` itself (shared by several other commands) to support a forced mtime too — deliberately deferred to its own future increment rather than shipped as a metadata-only version that wouldn't actually deliver reproducible image digests (see `docs/design/0199`), and, as a shared prerequisite for that same still-deferred `--timestamp` flag, real forced-mtime support landed in `oci_layer::export`/`export_tree` and `oci_dockerfile::commit_layer`/`squash_layer` themselves (checked directly against real buildah's own `destinationTimestamp` plumbing all the way down to `containers/storage`'s own per-header `ModTime` override) — two builds of byte-identical content committed at genuinely different real wall-clock moments now produce the exact same layer digest when given the same forced timestamp, verified directly; and now `ociman build --timestamp <SECONDS>` itself (0210) — the actual CLI flag, threading its parsed value through every history-recording call site via `StageContext.forced_mtime` (the same struct that already carries `.dockerignore` for the identical reason) and `oci_dockerfile::record_layer`/`record_empty_history`'s own new `forced_created` parameter: every new history entry and the image's own top-level `created` get exactly the given value instead of the real, live build time, while an already-cached layer's own original timestamp is left completely untouched (a cache hit never re-records anything) — verified end to end: two separate `--no-cache --timestamp` builds of identical content, run seconds apart, now produce the exact same image digest (see `docs/design/0209`-`0210`), and `ociman build --dns`/`--dns-search`/`--dns-option` (0299), closing `0298`'s own "still ahead" — the identical real `podman build --dns`/`--dns-search`/`--dns-option` semantics `ociman run`/`create` already got, reusing `oci_runtime_core::resolv_conf::write_resolv_conf` unchanged for a third time; ported real buildah's own checked-directly `--dns none` special case exactly (`~/git/podman/vendor/go.podman.io/buildah/run_linux.go`: exactly one value, case-insensitive `none`, skips `/etc/resolv.conf` entirely; any other combination still creates it), and confirmed (not assumed) that this project's own build cache — like real podman's/buildah's own — has never accounted for ambient per-build flags such as `--dns`/`--add-host` in its cache key, a real, pre-existing, faithfully-shared limitation rather than a newly introduced one (see `docs/design/0299`), and `ociman build`'s own `COPY`/`ADD --exclude=<pattern>` (0340) — a real, `.dockerignore`-syntax per-instruction exclude, matching real BuildKit's own `AddStrings` flag exactly (repeatable, applies regardless of `--from=`, unlike `.dockerignore` itself, which never applies to a `--from=<stage>`/`--from=<external-image>` source at all) — combined with the build's own `.dockerignore` (when present) by concatenating both raw pattern lists and recompiling once, the only way to preserve `!`-negation ordering across two independently-sourced pattern lists (see `docs/design/0340`) |
| 5 | erofs/mount/BLS; `ociboot install to-disk`; dracut module; QEMU boot test | in progress — `oci-erofs` builds real, verified-deterministic erofs images via `mkfs.erofs`, seals/verifies them with real fs-verity ioctls, and has a detached dm-verity fallback via `veritysetup`; `oci-bls` reads/writes the real grubenv block and Type #1 BLS entries, scans `/loader/entries/` as a directory, implements the real spec's own boot-counting filename convention, and sorts entries per the real spec's own "Sorting" section including full UAPI.10 version comparison (all verified against the real uapi-group spec/tools); `oci-mount` attaches/detaches real loop devices; `ociboot` now has two real subcommands, `list` and `grubenv` (a real, pure-Rust `grub-editenv` equivalent, verified byte-for-byte compatible with the real binary — see `docs/design/0061`-`0066`, `0070`-`0071`, `0087`, `0125`); `oci-bls` also now has a real kernel command-line parsing/editing primitive (`cmdline`, a direct port of real bootc's own `bootc-kernel-cmdline` crate, cross-checked against its own test suite including its own "pathological" quoting edge cases, plus a real kargs-diffing function porting real bootc's own `compute_apply_kargs_diff` — see `docs/design/0137`-`0138`), no CLI surface of its own yet; `ociboot`'s third real subcommand, `build-image`, is the first genuinely new slice of `install to-disk` itself — builds a real, deterministic, sealed-ready erofs deployment image straight from an already-pulled OCI image reference (never touching a real disk/partition table at all), reusing the exact same already-extracted-rootfs cache `ociman run` shares across every container of the same image (`cache_root` moved from `ociman`-private into `oci-store` itself so both binaries share the one cache, rather than each independently extracting the same image) — `timestamp`/`uuid` (both real `mkfs.erofs` inputs a caller must supply for a reproducible image) are derived from the image's own `created` field and manifest digest respectively, verified byte-identical across two separate builds of the same image with a real delay in between (see `docs/design/0200`), and `ociboot build-image --seal` for real fs-verity sealing right after the image is written, printing its own real fs-verity digest — deliberately opt-in (real bootc/composefs always seal, but this project's own `--output` destination isn't always a real fs-verity-capable filesystem the way a real target installation disk almost always would be), verified directly: the sealed file becomes genuinely immutable at the kernel level (a real write afterward fails with `EPERM`), and the printed digest is a real, non-placeholder 32-byte hash (see `docs/design/0201`), and that same `--seal` now falling back to a real, detached dm-verity hash tree (`<output>.verity`, via the already-existing `oci_erofs::dmverity`) whenever fs-verity itself isn't supported on the destination filesystem (`EOPNOTSUPP` specifically, any other error still a hard failure) — verified directly: the printed root hash verifies successfully against the real hash tree via a real `veritysetup verify` subprocess call, two separate builds of the same image produce the identical root hash, and (unlike fs-verity) the data file itself stays perfectly ordinary and writable afterward (see `docs/design/0202`), and `ociboot bless` for the real UAPI Boot Loader Specification's own "Boot counting" success half — checked directly against the spec's own fetched text ("the boot loader...decreases this count by one [but] if the operating system considers the boot as successful, it removes the counter altogether"): the *decrement* on every attempt is a real boot loader's own job (already handled by `grub2-bls`/`systemd-boot` long before `ociboot` ever runs), so this owns only the one half the spec explicitly assigns to "the operating system" — stripping a real entry's own counting suffix entirely (a real, atomic rename) once its boot is confirmed successful, a harmless idempotent no-op for an already-good entry (see `docs/design/0203`); and `ociboot-init mount` (0246) — the boot-time verify-and-mount itself, milestone 5's actual core begun: `ociboot.image=`/`ociboot.verity=` kargs parsed with the same shared `oci-bls` cmdline parser (image name traversal-guarded like `ocibox`'s own rule), the fs-verity digest enforced byte-for-byte *before* anything is attached when the karg is present (unsealed-but-expected and mismatches are hard boot errors; verity-less boots mount unverified with a real warning, since `--seal` is opt-in), then a real read-only loop attach + erofs mount via the shared `oci-mount` syscalls, loop device released on mount failure — verified end to end including the real privileged path (a real loop-mounted deployment whose tree reads back and rejects writes) under the workspace's opportunistic sudo gate; and the writable view now assembled too (0250, `--state-dir`): a real `/etc` overlay whose upper half persists on the state partition while the image stays sealed, `/var` bound from state, fresh `nosuid,nodev` tmpfs `/run`+`/tmp`, required-directory checks with clear errors, and a failure path that genuinely tears everything down again — including a real lazy-unmount-races-loop-detach leak the test's own leak assertions caught, fixed with synchronous deepest-first unmounts and two small new shared `oci-mount` primitives (`unmount`/`unmount_detach`) rather than a new dependency, plus a second, closely related real kernel race the same flaky-test-not-inspection way: `LOOP_CLR_FD` reporting success while only *scheduling* the clear (`systemd-udevd` transiently probing the just-changed device, observed directly), now confirmed via a genuine `LOOP_GET_STATUS64` poll (`oci_mount::wait_until_detached`) rather than trusted on the ioctl's return code alone — verified against real mounts end to end (edits landing host-visible in the state upper, root-rejected writes on the sealed base) and 10/10 clean repeats of a test that used to fail intermittently; switch-root, state-partition mounting, the dm-verity sidecar fallback, real partitioning/bootloader installation, `--karg` emission into BLS entries, and the dracut module are still ahead |
| 6 | upgrade/switch/rollback/status/gc; /etc merge; boot counting; layered mode | in progress — the actual prerequisite a real `status` needs, found by researching real bootc's own implementation directly (`bootc_composefs::status`) rather than guessed: `ociboot build-image` now writes a real, deterministic `<output>.origin.json` sidecar recording which OCI image reference/digest/version produced each deployment (`ociboot`'s own design, not a literal port of bootc's own ini-format `.origin` file — see `docs/design/0220`), written silently (internal bookkeeping, verified never to have added a second, competing line of stdout output to an already-tested command) and atomically (the same temp-file-plus-rename technique `oci_store`'s own pointer files already use). `ociboot status <IMAGE>` (0222) is that record's own first real reader — reports `image_reference`/`image_digest`/`image_version`/`built_at` for a given deployment image, in both human-readable and `--json` form (finally wiring up `ociboot`'s own previously-dead `--json` global flag), a real, honest "unknown" rather than a misleading `1970-01-01T00:00:00Z` for the one documented `built_at: 0` sentinel — deliberately *not* yet a full bootc-style booted/staged/rollback report, which needs a real BLS-entry-to-deployment link and a real boot-time `/proc/cmdline` convention, neither of which exists yet; `upgrade`/`switch`/`rollback`/`gc`, `/etc` merge, the `grubenv`-based boot-counting protocol, and layered mode are all still ahead |
| 7 | `ocicri` (critest subset), `ocibox` | in progress — a shared prerequisite done first while scoping `ocibox create` (0204): `oci_registry::resolve_or_pull`, the "look an image up locally, pull it according to a policy if needed" decision tree every one of `ociman run`/`create`/`build` already relied on, moved out of `ociman`-private code into the shared `oci_registry` crate itself, a pure, verified-unchanged move, so `ocibox`/a future `ocicri` ImageService wouldn't have to reimplement it; `ocibox create --image`/`--name`/`--pull` is `ocibox`'s own first real subcommand — resolves (pulling per the now-shared policy) an image and extracts a real, dedicated, writable rootfs for a named box (deliberately *not* through `oci_store`'s own shared, read-only `rootfs_cache`, which would let a write inside one box corrupt every other container of the same image — a pet container needs its own independent copy), refusing an already-used name and leaving no half-created box directory behind on a failed pull — studied real `distrobox create`'s own actual implementation directly (`~/git/distrobox`) first: its real scope (X11/Wayland/audio/nvidia passthrough, init-hooks, additional packages, cloning) is large, so this is deliberately just the resolve-and-extract slice, actually *launching* a box (`ocibox enter`, real namespaces/mounts/home-directory bind mount — confirmed reusable as already-shared, already-tested library code via `oci_runtime_core::launch::create`/`exec_fifo::signal_start`, no ociman-private detached-keeper machinery needed) still ahead; `ocibox list`/`ls` and `ocibox rm` round out the family enough to actually manage what `create` makes — `list` sorted by name matching real `distrobox list`'s own stable order (narrower than its real column set, since this project's own boxes aren't real running containers yet), `rm` refusing an unknown name; found and fixed a real path-traversal hazard while implementing `rm` — its own `name` argument needed the exact same charset validation `create` already applies before ever building a path from it, without which `ocibox rm ../../etc`-shaped input could have reached arbitrary paths outside the box storage root entirely, confirmed fixed directly with a real canary file just outside it that survives the attempt untouched (see `docs/design/0204`-`0206`), and `ocibox enter` — actually launching a box, via the exact same shared `oci_runtime_core::launch`/`Bundle`/`validate` two-phase lifecycle `ociman run`/`ocirun run` already use, with zero new namespace/mount/launch code of its own: a single foreground fork+exec+wait per invocation (matching `ocirun run`'s own simplest model, deliberately *not* yet real `distrobox enter`'s own persistent-background-container-across-sessions model, a materially bigger feature left explicitly deferred), defaulting to `/bin/bash`/`/bin/sh` when no command is given, bind-mounting a real existing `$HOME` when set, and forwarding the launched command's own real exit code as `ocibox enter`'s own exit code the exact same way `ocirun run`'s own `cmd_run` already does (a real bypass of `oci_cli_common::run_main`'s usual `Ok(())`-means-success mapping) — the box's own rootfs persists a write across two separate `enter` invocations even though the container process itself does not, verified directly (see `docs/design/0207`), and `ocibox rm --all` for removing every existing box in one call, matching real `distrobox rm --all` — mutually exclusive with an explicit name (a clear error either way rather than an ambiguous silent guess), every box still attempted even if an earlier one fails, and a real, silent no-op on an already-empty store (see `docs/design/0208`), and `ocibox enter` — actually launching a box, via the exact same shared `oci_runtime_core::launch`/`Bundle`/`validate` two-phase lifecycle `ociman run`/`ocirun run` already use, with zero new namespace/mount/launch code of its own: a single foreground fork+exec+wait per invocation (matching `ocirun run`'s own simplest model, deliberately *not* yet real `distrobox enter`'s own persistent-background-container-across-sessions model, a materially bigger feature left explicitly deferred), defaulting to `/bin/bash`/`/bin/sh` when no command is given, bind-mounting a real existing `$HOME` when set, and forwarding the launched command's own real exit code as `ocibox enter`'s own exit code the exact same way `ocirun run`'s own `cmd_run` already does (a real bypass of `oci_cli_common::run_main`'s usual `Ok(())`-means-success mapping) — the box's own rootfs persists a write across two separate `enter` invocations even though the container process itself does not, verified directly (see `docs/design/0207`), and `ocibox ephemeral` — a disposable box under a real, random, dependency-free-generated name (`ocibox-<hex>`, no `rand` crate needed, the same technique `ociman`'s own `short_id` already uses), entered once and always removed again afterward even on a nonzero exit or an outright `enter` failure, matching real `distrobox ephemeral` exactly — a pure composition of `create`/`enter`/`rm` with zero new namespace/mount/launch code of its own (see `docs/design/0211`), and `ocicri`'s own first real slice (0212): a genuine, running gRPC server (not a placeholder) over a real Unix socket, using the real, vendored Kubernetes CRI v1 protobuf schema (`k8s.io/cri-api`, from real `cri-o`'s own vendor tree — a new shared `oci-cri-types` crate, since `oci-tools-tests` needs the identical generated client stubs too) — `RuntimeService.Version` (kubelet's own first connectivity check against any runtime) is genuinely implemented, every other one of `RuntimeService`'s 33 other RPCs (and all of `ImageService`, not registered yet) returns a real, honest `Status::unimplemented` naming itself rather than silently accepting a request this project can't act on; `tonic`/`tokio`/`prost` are new dependencies confined entirely to `ocicri`/`oci-cri-types` — every other binary still links neither, so this project's own "beat every benchmark, especially startup time" pillar is unaffected everywhere except this one, deliberate, long-lived-server exception; verified with a real generated `tonic` client connecting over a real Unix socket, not just in-process unit tests (see `docs/design/0212`), and `ImageService`'s own `ListImages`/`ImageStatus` (0213), reusing this project's own already-tested `oci_store` primitives directly (the same ones `ociman images`/`ociman inspect` already use) — a shared prerequisite moved image-resolution logic ("resolve by tag, or a real/short image ID") out of `ociman`-private code into `oci_store` itself first, since CRI's own `ImageSpec.image` field is routinely a bare digest/ID, verified as a genuine zero-behavior-change move (every one of `ociman`'s own existing tests exercising it passes completely unmodified); both new RPCs' own edge-case semantics (an unresolvable image is a real, empty response, never an error; a filter resolves just the one named image) checked directly against real `cri-o`'s own source, not guessed from the proto's prose (see `docs/design/0213`), and `PullImage` (0214), mapping cleanly onto the same already-shared `oci_registry::pull_unconditionally` — its own real, unconditional network pull runs on a `tokio::task::spawn_blocking` thread rather than directly inside its own `async fn` (this project's own registry client is a plain, synchronous, blocking one, shared unchanged with every other binary), verified directly: a slow, real pull attempt against an unreachable host never blocks an unrelated `Version` call on the same server in the meantime; a genuinely successful pull-and-store round trip verified by hand against the real, live `docker.io/library/busybox:latest` registry (production always uses `tls_verify: true`, which can't reach this project's own local mock-HTTP-registry test helper) — inline kubelet-supplied `AuthConfig` credentials aren't honored yet, only the same on-disk auth file `ociman login` already populates (see `docs/design/0214`), and `RemoveImage` (0215), rounding out `ImageService`'s own basic CRUD — a small, direct implementation matching a real, checked-directly rule genuinely *different* from `ociman rmi`'s own: removing any one tag (or ID) resolving to an image removes *every* real reference sharing that same manifest digest, unconditionally (no `--force`-style ambiguity gate at all, confirmed against both the real proto's own doc comment and real `cri-o`'s own source), and is a real, silent, idempotent no-op when nothing resolves at all, matching the real proto's own documented contract exactly (see `docs/design/0215`), and `ImageFsInfo` (0223), rounding out `ImageService` apart from the feature-gated, opt-in `StreamImages` streaming alternative (still a real, honest `Status::unimplemented` — real `cri-o` itself may not even implement it) — a real directory-tree-walk-based filesystem usage report, checked directly against real `cri-o`'s own `getStorageFsInfo`/`utils.GetDiskUsageStats`: `image_filesystems` reports the real content-addressed blob store's own usage, `container_filesystems` the real, shared, extracted rootfs cache `ociman run`/`ociboot build-image` themselves already extract into, both via a new, shared `oci_store::dir_stats` (the same real, hardlink-deduplicated walk `dir_size`/`ociman prune` already rely on, extended to also report a real file count in the same pass — deliberately more correct than real cri-o's own cruder, non-hardlink-aware walk), and a real, honest all-zero report (not an error) for a directory that legitimately doesn't exist yet (a fresh store nothing has `run`/`build-image`d anything on yet), a deliberate divergence from real cri-o's own test suite, which does expect an error there (see `docs/design/0223`), and `RuntimeService.Status` (0228), the second genuinely implemented `RuntimeService` RPC after `Version` — checked directly against real cri-o's own `runtime_status.go`: a real `RuntimeReady=true` (matching cri-o's own identical hard-coded reasoning — answering the RPC at all is the only proof either implementation checks) alongside a real, honest `NetworkReady=false` with a reasoned message (unlike real cri-o's own live CNI-plugin poll, this project sets up no container networking of its own at all yet — see `docs/design/0147`), exactly one default runtime-handler entry with real, honestly-`false` feature bits (no configurable runtime-handler concept, no recursive-read-only-mounts/user-namespace support implemented anywhere yet), and a real, non-fabricated `info` map (the same `runtimeName`/`runtimeVersion` `Version` itself already reports) only when `verbose` is set, and `RuntimeConfig`/`UpdateRuntimeConfig` (0229) — `UpdateRuntimeConfig` a real, unconditional no-op matching real cri-o's own identical implementation exactly (the pod-CIDR-pushing mechanism it exists for is vestigial since Kubernetes removed the kubenet network plugin years ago), and `RuntimeConfig` reporting the real cgroup driver `ociman run`/`create` actually, unconditionally uses today (systemd — checked directly in this project's own source, not assumed; `ocirun` itself uses plain cgroupfs instead, but that's the low-level runtime layer, not what a kubelet's own `RuntimeConfig` call is asking about), matching real cri-o's own checked-directly default too, and `ListMetricDescriptors` (0231) reporting a real, honest empty list — checked directly against real cri-o's own implementation (entirely static/config-driven, defaults to almost nothing — one always-on descriptor — on a real, unconfigured install), `ocicri` reports even less since it has no metrics-collection machinery of its own anywhere yet, and advertising a descriptor no later `ListPodSandboxMetrics` call could actually populate would be a real, false claim, and the full pod-sandbox lifecycle (0233) — `RunPodSandbox`/`StopPodSandbox`/`RemovePodSandbox`/`PodSandboxStatus`/`ListPodSandbox`, a real, persistent, record-keeping state machine (one atomic JSON record per sandbox, surviving a real `ocicri` kill/restart, verified directly) with real CRI semantics checked against real cri-o's own source rather than guessed from the proto: real cri-o's own `k8s_<name>_<namespace>_<uid>_<attempt>` pod name and 64-hex ID shape, kubelet-default `io.kubernetes.pod.*` labels populated when a client (`crictl`) didn't, a duplicate request (same metadata) returning the *existing* sandbox's ID as a success (real kubelet retries depend on this — cri-o's own `reservePodNameOrGetExisting` branch), idempotent stop, silent not-found successes for stop/remove versus a real gRPC `NotFound` for status (cri-o's own genuinely different per-RPC rules, each checked), prefix-based ID resolution matching its truncindex, and ANDed list filters (id/state/label-selector) — deliberately *no* infra ("pause") process or pinned namespaces yet, documented honestly rather than half-implemented: real cri-o's own `drop_infra_ctr` default means an ordinary real sandbox has no live process of its own either, and the namespace pinning is deferred until this project grows real pod networking and a `CreateContainer` (still a real, honest `Status::unimplemented`) that could even ask to join (see `docs/design/0233`), and the `CRIListStreaming` streaming list variants (0234) — `StreamPodSandboxes` and `StreamImages` (completing `ImageService` outright), each sharing the *exact* same factored-out filtered-list computation its plain list RPC uses (a pure, verified-unchanged move) and streaming it in chunks of real cri-o's own `streamChunkSize = 3000` with zero messages for an empty result — 0213's own guess that real cri-o "may not even implement" these was re-checked directly and found outdated (its `image_list.go`/`sandbox_list.go` genuinely implement all three), while `StreamContainers` stays honestly unimplemented alongside its own still-unimplemented `ListContainers` (see `docs/design/0234`), and the container lifecycle's own record slice (0236) — `CreateContainer`/`ContainerStatus`/`ListContainers`/`RemoveContainer` as the second family of real, persistent, restart-surviving records (their save/load/prefix-resolve mechanics factored out of the sandbox store into one shared, generic `records.rs` the moment a second user existed, rather than duplicated), with real cri-o-checked semantics throughout: cri-o's own `k8s_<ctr>_<pod>_<ns>_<uid>_<attempt>` container name (pod half from the *request's* own `sandbox_config`, exactly like cri-o), duplicate-create returning the existing ID, a stopped sandbox refusing new containers (`FailedPrecondition`, cri-o's own message), an unpulled image a clear `NotFound` (kubelet always `PullImage`s first — no pull-policy input exists on this RPC at all), idempotent forceful remove vs a real `NotFound` status, ANDed list filters including cri-o's own id+sandbox `HasPrefix` cross-check, and `RemovePodSandbox` now forcibly removing the sandbox's own containers (the proto's contract, previously moot with no containers possible) — every record honestly `CONTAINER_CREATED`: `StartContainer` (where the real `oci_runtime_core::launch` reuse lands) is its own bigger, later increment, so the only state this slice writes is the only one that can truthfully exist (see `docs/design/0236`), and `CreateContainer` now preparing a real, *verified launch-ready* OCI bundle at create time (0237), matching real cri-o's own create-time storage/spec preparation exactly: a dedicated writable rootfs (shared `oci_layer::apply`, `ocibox create`'s own never-share-a-writable-rootfs reasoning) plus a generated `config.json` (the same `Spec::example().into_rootless` + podman-default-caps + default-seccomp base every other launched container here gets) whose process half ports real cri-o's `SpecSetProcessArgs` command/args-vs-Entrypoint/Cmd merge rule for rule — including its verbatim `no command specified` error, deliberately costing no extraction — and image-env-then-kube-env ordering with `ociman`'s own checked `PATH` fallback (0194), every bundle round-tripped through the real `Bundle::load`+`validate::validate` a launch starts with before create ever succeeds, with symmetrical cleanup everywhere (failed create, failed record write, `RemoveContainer`, sandbox cascade) — `StartContainer` itself explicitly scoped out: `launch`'s own fork-safety contract (single-threaded caller) is unsatisfiable from a tokio server, so the conmon-shaped helper-process design gets its own note (see `docs/design/0237`), and that design now landed (0238): `StartContainer`/`StopContainer` run **real container processes** via a per-container launcher-keeper — a fresh, single-threaded re-exec of `ocicri` itself (`__launch`, the same self-re-exec technique real `runc init` uses; nothing external invoked) that launches the 0237 bundle through the same shared `run_reporting_pid`/systemd-scope path `ociman run` uses, `setsid`-detached so running containers and their eventual real exit codes survive an `ocicri` restart (conmon's own lifetime), speaking a tiny atomic `pid`/`exit.json`/`start-error` protocol in the bundle dir — start requires `CONTAINER_CREATED` (cri-o's verbatim "is not in created state" error), stop is cri-o's checked semantics exactly (idempotent unknown/exited, never-started settles at `-1`, running gets SIGTERM + `timeout` + SIGKILL, async-polled off the tokio workers), status/list reconcile against the keeper's own exit records (real `started_at`/`finished_at`/`exit_code`, `Completed`/`Error` reasons), and every forceful path (`RemoveContainer`, both sandbox cascades) SIGKILLs a live container first — verified end to end over a real socket including a real TERM-trap container whose own exit code (42) proves the graceful path, plus two real kernel/busybox behaviors the tests had to learn, documented in the note (see `docs/design/0238`) — the second of which (containers had no real `/dev/null` at all; a shell's own `> /dev/null` was silently creating a regular tmpfs file) is now fixed for **every** binary at once in shared runtime-core (0239): a tmpfs `/dev` gets the OCI runtime spec's own mandated default devices (real crun's `needed_devs` table byte for byte — null/zero/full/tty/random/urandom, c 0666, via real `mknod` with the same explicit umask-bypassing re-chmod crun does, falling back on `EPERM` to the host-node bind mounts real rootless runc/crun use) and standard symlinks (`fd`/`stdin`/`stdout`/`stderr`/`core`/forced `ptmx`), only ever for a container-managed tmpfs `/dev` (a bind-mounted host `/dev` is left alone), never second-guessing anything the image provides — verified by a real rootless container proving `test -c` on all six, correct symlink targets, and the real read-back semantics (`cat /dev/null` empty after a write; `/dev/zero` yields real NUL bytes), with `ci/bench.sh` re-run since this is genuinely hot-startup-path work: the gap holds (both sides now do identical device setup — see `docs/design/0239`)), and `ExecSync` (0240) — kubelet's own exec liveness/readiness probes, via a second hidden re-exec entry point (`__exec`) building its `ExecRequest` exactly like `ociman exec` does (everything from the target's own bundle spec; the CRI request has no per-call overrides), stdout/stderr captured over real pipes, the helper `setsid`-grouped so a timeout SIGKILLs the whole tree — semantics checked against real cri-o verbatim (unknown/no-living-process `NotFound`s, "exec command cannot be empty", and the timeout being a *successful* `-1`/"command timed out" response, cri-o's own explicit kubelet-prober reasoning), including a real pre-exec race found and fixed along the way (the pid file exists before the container has exec'd; an exec landing in that window joined a half-set-up world — the `__exec` helper now gates on the target's own cmdline flip, see `docs/design/0240`)), and container stats (0241) — `ContainerStats`/`ListContainerStats`/`StreamContainerStats`, real live cgroup-backed usage via the same shared `oci_runtime_core::cgroups` readers `ociman stats` uses, mapped with real cri-o's own cgroup-v2 formulas checked field by field (`usage_core_nano_seconds` from `cpu.stat`, `working_set_bytes` = current−`inactive_file`, raw `usage_bytes`, `rss=anon`, pgfaults, `available_bytes` only under a real limit, writable-layer bytes via the same hardlink-aware `dir_stats` `ImageFsInfo` uses) — `usage_nano_cores` deliberately never fabricated (cri-o derives it from two samples; kubelet computes rates itself), and absence over fabrication throughout: no live cgroup (created/exited/no-D-Bus-fallback) means no stats row, never a zero one (see `docs/design/0241`)), and the CRI log path (0242) — `kubectl logs`' own actual data source: a container created with kubelet's `log_directory`+`log_path` streams stdout/stderr through pipes into a dedicated logger process (conmon's other half, forked not threaded — `launch`'s fork-safety contract again) writing the real CRI logging format (`<RFC3339Nano> <stream> <P|F> <content>`, timestamp formatter unit-tested against the CRI doc's own example byte for byte, oversize lines cut into `P` chunks at conmon's own 8192 cap — checked *before* the newline, a real ordering subtlety the unit test caught), complete no later than the exit is observable, path reported by `ContainerStatus`, no log config meaning no logger at all — and that deferred `ReopenContainerLog` now landed too (0243): the same control-FIFO mechanism real cri-o drives real conmon with (checked directly — a command byte into a `logger-ctl` fifo in the bundle dir; the nonblocking write doubles as a real logger liveness check via `ENXIO`), the logger's detached control thread swapping in a fresh create-or-append handle (never truncate: un-rotated content must survive), verified end to end with a real rename-away rotation — old lines stay in the renamed file, fresh ticks land in the new one (see `docs/design/0242`-`0243`)), and `STOPSIGNAL` honored everywhere it should be (0244) — `ociman stop` (and `restart`) now default to the image's own declared stop signal exactly like real `docker stop`/`podman stop` (explicit `--signal` still wins; resolution never fails a stop — an image since `rmi`'d falls back to TERM, honestly noted), and `ocicri StopContainer`'s graceful phase sends it too, captured onto the container record at create time with real cri-o's own checked garbage-tolerant TERM fallback — both proven end to end by a two-trap container whose distinct exit codes (USR1→43, TERM→21) reveal exactly which signal actually arrived (see `docs/design/0244`), and `RuntimeService.UpdateContainerResources` (0251) — real, live cgroup writes for a running container via the exact same shared `oci_runtime_core::cgroups::plan_resources`/`apply` pair `ociman update`/`ocirun update` already use, composed rather than reimplemented; a created-but-never-started container is a real `FailedPrecondition` rather than a silent no-op (this project's own `CreateContainer` doesn't set up a live cgroup the way real cri-o's does, so there is honestly nothing to update yet), and the request's own explicit `memory_swap_limit_in_bytes` is honored directly rather than replicating real cri-o's own checked implementation, which curiously never reads that field at all — verified against the real `memory.max`/`cpu.max` cgroup files afterward, not just the RPC's own empty success response (see `docs/design/0251`), and `ocibox export --bin` (0252) — writes a small, real, executable wrapper script that routes an exported binary's own invocations through `ocibox enter`, matching real `distrobox export --bin`'s own actual shell implementation (checked directly, `~/git/distrobox/internal/inside-distrobox/assets/distrobox-export`) field for field (the same `distrobox_binary`-style marker comment/single-quoted-binary-path template, `--delete`'s own identical foreign-file safety check), deliberately not yet `--app` desktop-entry export (needs real desktop-file/icon handling this project has none of yet) — and one real, honestly-documented divergence: since `ocibox enter` has no persistent per-session `$CONTAINER_ID` a real `distrobox export` run *inside* a box could detect, this instead runs from the host with an explicit `--box` naming which one to route through — verified end to end including actually *running* the generated wrapper (a real `sh` invocation of the exported binary inside the real box, not just inspecting the file), a missing binary/unknown box both clear errors, and `--delete` refusing a foreign, non-exported file at the same destination (see `docs/design/0252`), and `RuntimeService.StreamContainers` (0253) completes the `CRIListStreaming` family `StreamPodSandboxes`/`StreamImages` started (0234) — the exact same filtered-list computation `ListContainers` already uses, streamed through the one shared chunking helper all three now share, no new logic of any kind (see `docs/design/0253`), and `RuntimeService.UpdatePodSandboxResources` (0254), `UpdateContainerResources`'s own pod-level sibling — checked directly against real cri-o's own `server/sandbox_update_resources.go`: beyond resolving the sandbox (a real `NotFound` when it doesn't exist), real cri-o's own implementation does nothing to a sandbox's own cgroup directly at all, delegating every actual change to its own optional NRI plugin framework (a real no-op with none configured, the only setup either project's own CI ever runs) — this project has no NRI concept at all, so this is honestly that exact same no-op once the sandbox is confirmed to exist, not a fabricated cgroup write `ocicri` has nowhere to route anyway (an ordinary sandbox here has no live process or cgroup of its own, `docs/design/0233`) (see `docs/design/0254`), and `ListPodSandboxMetrics`/`StreamPodSandboxMetrics` (0255) now report the same real, honest empty answer `ListMetricDescriptors` already does — revisiting `ListMetricDescriptors`'s own earlier reasoning by actually tracing through real cri-o's own `server/sandbox_metrics_list.go`: with no `included_pod_metrics` configured (this project's own only real point of comparison, and real cri-o's own default too), its real, unconfigured answer is a plain, successful, empty list, never an error — so the two RPCs' own earlier `Status::unimplemented` was more conservative than real cri-o's own actual behavior warranted, corrected here to match it exactly (see `docs/design/0255`), and `GetContainerEvents` (0258) — checked directly against real cri-o's own `server/container_events.go`: entirely gated behind its own `enable_pod_events` config toggle, which defaults to `false` (Go's own zero value, no explicit default assignment), so a real, unconfigured install's own implementation is one line, `return nil`, before ever touching its own event broadcaster — a real, clean, zero-message stream, never blocking to wait for an event. This project has no event-generation machinery (or an `enable_pod_events`-equivalent toggle) anywhere, so the honest answer matches that identical default exactly rather than a hard `Status::unimplemented` real cri-o's own default install would never actually return here (see `docs/design/0258`), and `CheckpointContainer` (0259) — checked directly against real cri-o's own `server/container_checkpoint.go`: `EnableCriuSupport` actually *defaults* to `true` but is force-disabled again at startup unless a real `criu` binary is found on `$PATH` (essentially no host by default), so the practical real answer is a bare Go error, never wrapped in a `status.Error`, surfacing over real gRPC as `codes.Unknown` — this project has no CRIU integration at all (a structurally different, deliberately out-of-scope reason), but returns that exact same real error shape (`codes.Unknown`, "checkpoint/restore support not available") rather than a generic `Status::unimplemented`, since that *is* what a real, unconfigured install actually returns here too (see `docs/design/0259`), and `PodSandboxStats`/`ListPodSandboxStats`/`StreamPodSandboxStats` (0262) — a genuinely different shape than the two RPC families just above: checked directly, real cri-o's own implementation isn't gated behind any disabled-by-default config flag here at all, since its own sandboxes always have a real infra ("pause") container and cgroup to read from; this project's own sandboxes deliberately have neither (`docs/design/0233`), so this instead applies `ContainerStats`'s own already-established rule (0241) at the sandbox level — real `Attributes` (id/metadata/labels/annotations) reported in full, `Linux` always honestly absent rather than a fabricated zero, with an unknown sandbox a real `NotFound` (see `docs/design/0262`), and two real, previously-unnoticed hostname bugs found and fixed by tracing every spec-building call site's own handling of the shared `Spec::example()` template's hardcoded `"ocirun"` default (0292): every `ocibox enter`'d box, regardless of its own real name, used to report the literal hostname `ocirun` (`enter_spec` never overrode it at all) — now defaults to the box's own name, the same convention `ociman run` already established; and every `ocicri`-managed container did too (`bundle.rs`'s own module doc comment had already named `PodSandboxConfig.hostname` wiring as out of scope) — now wired to the sandbox config's own real hostname if given, else the sandbox id's own first 12 hex chars, matching real cri-o's own `getHostname` exactly, plus the matching `HOSTNAME=` process env var real cri-o's own `AddProcessEnv` call site also sets (see `docs/design/0292`), and a real, synthesized `/etc/hosts` for every `ocicri`-managed container (0296) — `bundle.rs`'s own module doc comment had explicitly named this as out of scope until now; reuses `ociman run`'s own already-verified `write_etc_hosts`/`parse_extra_host` (`0147`), moved out of `ociman`-private code into `oci_runtime_core::etc_hosts` (converted to this crate's own established `std::io::Result` convention rather than pulling in a new `anyhow` dependency) the moment this second, unrelated caller needed the identical primitive — the sandbox's own real hostname (`0292`) maps to `127.0.0.1`, matching real cri-o's own non-host-network default and `ociman run`'s own `--network=none`-shaped case exactly; verified the moved unit tests pass byte-for-byte identically in their new home, and `ociman`'s own full `run`/`build` test suites (181 tests) pass completely unmodified (see `docs/design/0296`), and a real `/etc/resolv.conf` for every `ocicri`-managed container (0297), closing `0296`'s own last "still ahead" — matching real cri-o's own checked-directly `ParseDNSOptions` exactly (`~/git/cri-o/internal/lib/sandbox/infra.go`): an explicit `PodSandboxConfig.dns_config` is synthesized (`search`/`nameserver`/`options`, in that real order), else the real host's own `/etc/resolv.conf` is copied verbatim — meaningful, not cosmetic, since this project's own containers already share the host's real network namespace unmodified, so the host's own real nameservers genuinely are reachable from inside; deliberately a new, simpler `oci_runtime_core::resolv_conf` primitive rather than porting real podman's own richer, namespace-aware `libnetwork/resolvconf` package, whose extra complexity (loopback-nameserver filtering, `KeepHost*` merge modes) exists for a container with its own private network namespace, a case this project has no equivalent of at all (see `docs/design/0297`), and `ocicri CreateContainer` support for `ContainerConfig.mounts` (0304), closing `0237`'s own deferred "CRI mounts" gap — a real, deliberately narrow first slice: a plain OCI bind mount (`container_path`/`host_path`/`readonly`), matching the exact same `Mount{Type: "bind", Options: ["rbind", ...]}` shape `ociman run -v` already builds, including real cri-o's own checked-directly "auto-create a missing host path" behavior (`os.MkdirAll`, not an error, despite the proto's own stricter-sounding doc comment) and its exact `["rbind", "rprivate"]` option pair for the private-propagation default; image volume mounts, non-default propagation, SELinux relabeling, recursive read-only, and UID/GID mappings are each a real, honest `Status::unimplemented` rather than a silent misinterpretation — verified end to end including a real started container reading a host-written file back through the mount via a real `ExecSync`, and writing one back the other direction, not just inspecting the generated `config.json` (see `docs/design/0304`), and a real, previously-shipped mount-source bug fixed (0305) — `build_cri_bind_mounts` used to call `create_dir_all` on every `host_path` unconditionally, which fails outright for the common real kubelet case of an already-existing single-file mount source (`/etc/localtime`, a ConfigMap key, ...); fixed to match real cri-o's own exact `resolveSymbolicLink` logic instead: an already-existing path (file or directory) is used exactly as given, a symlink is followed to its real target, and only a genuinely missing path is still auto-created as a directory — verified with a real started container reading both an existing single file's and a symlink's own real content back through the mount via a real `ExecSync` (see `docs/design/0305`), and `ocibox rm NAME [NAME...]` (0321), matching real `distrobox rm`'s own multi-name support — found and corrected two real, previously-incorrect behavioral assumptions along the way by reading real distrobox's own source directly rather than assumed (`~/git/distrobox/pkg/commands/rm.go`): an explicitly-named box that doesn't resolve at all is only ever a printed warning, never a hard, aborting error (real `distrobox rm` always exits `0` regardless of what happens in its own per-container removal loop, traced all the way to its own top-level `main()`), and `--all` is not mutually exclusive with explicit names at all — it simply takes full priority, silently ignoring them, proven with a real box removed by `--all` despite an unrelated name also given; a malformed/path-traversal name, this project's own separate defensive validation with no equivalent in real distrobox at all, deliberately remains a real, immediate, whole-call-aborting error throughout (see `docs/design/0321`), and `ocibox export --app` (0322) for desktop-entry export — closing `0321`'s own flagged next candidate, the most self-contained of `ocibox`'s remaining real gaps since it needs no new architectural decisions, only new file-generation logic reusing `export --bin`'s own already-established marker-comment/`--delete`-safety-check machinery; matches real `distrobox export --app`'s own core `Exec=`-rewriting mechanism exactly (checked directly against its own real shell implementation), including its own real "skip an already-exported app," "an absolute in-rootfs path works directly," and "strip `TryExec=` entirely" behaviors — deliberately without its own icon-copying half at first (see `docs/design/0322`) — closed next (0327): real icon search/copy/`Icon=` rewriting matching real `distrobox-export`'s own path-mapping rule exactly (`usr/share/`/`var/lib/flatpak/exports/share/` maps to `.local/share/`, with `pixmaps` renamed to `icons`), including its own narrow, checked-directly `Icon=/usr/share/`-only rewrite quirk deliberately preserved rather than silently improved beyond what real distrobox itself does (see `docs/design/0327`), and `ocibox export --app --export-label` (0328) for the exported application's own `Name=` label — matching real `distrobox export -el`'s own identical default exactly (not given at all appends " on <box_name>", `none` disables it, any other value is appended verbatim), applied only to a line that genuinely starts with `Name` rather than real distrobox's own cruder, unanchored `sed` rule, which would also touch an unrelated `GenericName=`/`Comment=` line merely containing the substring "Name" (see `docs/design/0328`), and `ocibox export --list-apps`/`--list-binaries` (0329) — listing every real, previously-exported application/binary from `--box`, matching real `distrobox export --list-apps`/`--list-binaries` exactly in shape, using this project's own already-established marker/box-name comment convention as a real, more precise per-box filter than real distrobox's own path-substring check against `$CONTAINER_ID` (see `docs/design/0329`), and `ocibox export --extra-flags` (0330) — extra flags appended to the exported command itself, matching real `distrobox export --extra-flags` exactly: for `--bin`, always inserted before the wrapper's own forwarded positional args; for `--app`, only inserted into the `Exec=` line when it already contains a real desktop-entry field code, a real, crude limitation of real distrobox's own `sed`-based implementation deliberately preserved rather than silently fixed (see `docs/design/0330`), and `ocibox create`/`ephemeral --hostname` (0344), matching real `distrobox create --hostname` exactly including its own real 64-character cap (`ErrHostnameTooLong`) — persisted on the box's own record and used by every later `enter`, overriding the box-name default `0292` already established (a deliberate, documented divergence from real distrobox's own real-host-hostname default, not revisited here since the override itself is unambiguous regardless of what the default resolves to); inherited by `ephemeral` too, matching real distrobox's own identical flag composition (see `docs/design/0344`), and `ocibox generate-entry` for a real, standalone desktop launcher entering a whole box, plus a right-click "Remove" desktop action, matching real `distrobox generate-entry` exactly for the `--all`/`--delete`/`--icon` surface, deliberately without its own real network-fetched per-distro-logo "auto" icon default (a fixed, standard freedesktop icon name instead -- see `docs/design/0364`), and `ocicri CreateContainer` now validates `ContainerConfig.linux.security_context`'s own `run_as_user`/`run_as_group`/`run_as_username` instead of silently ignoring them -- a real, previously-undetected divergence from a pod's own explicit intent (a container quietly ran as uid 0 regardless of what was actually requested), now a clear error for anything this rootless runtime cannot map yet (see `docs/design/0365`), and `ocibox create`/`ephemeral --home`/`-H` (0381) for using a custom host directory as a box's own `$HOME` instead of this process's own real host `$HOME`, matching real `distrobox create --home`/`-H` exactly (checked directly, `~/git/distrobox/pkg/commands/providers/{podman,docker}.go`) -- auto-created if it doesn't already exist (matching real distrobox's own `os.MkdirAll`, a real, immediate error if creation genuinely fails, never silently ignored, unlike this project's own already-established default-`$HOME`-missing case), verified end to end: a write inside the box genuinely lands on the custom host path, never the ambient `$HOME` (see `docs/design/0381`), and a real correctness fix for `ocicri`'s own `RemoveImage`: it now refuses to remove an image a real, persisted CRI container record still references (any container state, not just running, matching real cri-o's own `volumeInUse` check and real `container-libs/storage`'s own identical `DeleteImage` rule) instead of silently deleting every tag out from under a live container -- a stale doc comment claiming this couldn't be checked yet (written before `CreateContainer`/`StartContainer` became real in `0236`-`0238`) is fixed alongside the actual bug (see `docs/design/0383`), and `ocicri CreateContainer` now honors `security_context.readonly_rootfs` (`docs/design/0388`) -- previously silently ignored (`build_spec` used to force `root.readonly = false` unconditionally regardless of what the request actually asked for, the same shape of divergence `0365` already fixed for `run_as_user`), now genuinely setting `root.readonly` in the generated bundle spec to match a pod's own explicit intent, and `ocicri CreateContainer` now clearly rejects `security_context.privileged: true` (`docs/design/0389`) rather than silently ignoring it -- unlike every other unsupported field this codebase already checks, `privileged` was previously read nowhere at all, so a workload asking for privileged access used to get an ordinary, confined container instead with no error telling it so; real cri-o's own actual privileged-mode support is a large, many-part feature (every capability added, sensitive paths unmasked, seccomp/AppArmor/SELinux confinement dropped, every host device passed through) deliberately not attempted here, so a clear, honest `Status::unimplemented` replaces the previous silent no-op instead, and `ocicri CreateContainer` now writes `ContainerConfig.linux.resources` into the generated bundle spec at creation time (`docs/design/0390`) -- previously never wired in at all, so a container ran completely unconstrained by any CPU/memory limit until (if ever) a later, separate `UpdateContainerResources` call happened to arrive, a real divergence from ordinary Kubernetes QoS/resource-isolation expectations since kubelet normally never issues that RPC for a pod without in-place vertical scaling; reuses the exact same `spec.linux.resources` plumbing `ociman run`/`create` already write and the launcher already reads back out when actually starting a container, verified end to end with real `memory.max`/`cpu.max` cgroup files already holding the requested limits the moment the container starts, with no update call anywhere in the test, and `security_context.masked_paths`/`.readonly_paths` (`docs/design/0391`) -- previously never read at all, appended onto this project's own existing default masked/readonly path lists rather than replacing them, matching real cri-o's own checked-directly `specgen.AddLinuxMaskedPaths`/`AddLinuxReadonlyPaths` plain, non-deduplicating append; a real, explicit extra masked path is verified end to end as genuinely masked (bind-mounted over with `/dev/null`) inside a real started container, not just checked in the generated spec, since masking a file is a fresh bind mount entirely within this project's own unprivileged user namespace's authority, unlike the read-only cases' own real remount limitation, and `security_context.capabilities.add_capabilities`/`.drop_capabilities` (`docs/design/0392`) -- previously every CRI container got exactly the same hardcoded real `podman`-default 11-capability set regardless of what a pod's own `capabilities` requested; now merged via a shared `oci_runtime_core::identity::merge_capabilities` (moved out of `ociman`-private code the moment this second, unrelated caller needed the identical primitive, the same reasoning already established for `oci_runtime_core::etc_hosts`/`resolv_conf`, verified as a genuine, byte-for-byte-unchanged move for `ociman run --cap-add`/`--cap-drop` itself), including real cri-o's own checked-directly `add=["ALL"]` combined with an individual `drop` still applying that drop on top of the full set -- verified end to end with a real `/proc/self/status` bitmask read inside a running container, both adding and dropping a capability, and `ocicri CreateContainer` now applies `PodSandboxConfig.linux.sysctls` (`docs/design/0396`) -- a real, sandbox-(pod-)level CRI concept (not a container-level one at all), applied independently to each container's own generated spec since this project has no real infra process or shared per-pod namespaces of its own yet, the same "resolve a sandbox-level CRI concept per-container" precedent hostname/`/etc/hosts`/`/etc/resolv.conf` already established; reuses the exact same shared `oci_runtime_core::sysctl` validation `ociman run --sysctl` (`0395`) already relies on, so a `net.*` sysctl requested by a pod is just as safely rejected here -- verified end to end with a real sandbox-level sysctl genuinely applied inside a real started container, and `ocibox create`/`ephemeral --volume HOST-DIR:CONTAINER-DIR[:ro]` (`docs/design/0397`), matching real `distrobox create --volume` exactly -- a real, previously-missing extra bind mount, given once at `create` time and applied by every later `enter`, deliberately narrower than `ociman run --volume`'s own named-volume shorthand (`ocibox` has no volume-store concept to resolve one against at all), verified end to end including a real `:ro` write rejection, and `ocicri`'s own `CreateContainer`/`UpdateContainerResources` now honor `LinuxContainerResources.unified` too (`docs/design/0398`), reusing the exact same shared `oci_runtime_core::cgroups::apply_unified` `ociman run/create --cgroup-conf` established, applied with the same real, checked-directly higher-than-everything-else precedence, and `ocicri CreateContainer` now clearly rejects a non-zero `security_context.supplemental_groups` entry (`docs/design/0399`) -- flagged as a real, still-open gap in every one of `0388`-`0392`'s own "still out of scope" sections and previously read nowhere at all, so a pod's own explicit `securityContext.supplementalGroups` request was silently dropped; this rootless runtime can only ever map container gid 0 (the same constraint `run_as_group` already enforces), so a non-zero entry gets the identical clear, honest `Status::unimplemented` naming the offending value, while an empty list or one containing only `0` still succeeds exactly like a request with no security context at all, and `ocicri CreateContainer` now honors `resources.oom_score_adj` (`docs/design/0400`) -- previously read nowhere at all, wired onto the exact same `process.oom_score_adj` field `ociman run/create --oom-score-adj` (0394) already writes and `oci_runtime_core::oom::apply` already reads back out, so no new runtime primitive was needed, only this CRI-level plumbing; a real, checked-directly `~/git/cri-o` confirmation (`server/container_update_resources.go`'s own literal `// TODO(runcom): OOMScoreAdj is missing`) shows even real cri-o itself only ever applies this at creation time, never on a later `UpdateContainerResources` call, matching this project's own identical scope exactly -- verified end to end with a real started container's own live `/proc/1/oom_score_adj`, read via a real `ExecSync` (which needed its own real, previously-unconsidered `/proc/1/` rather than `/proc/self/` distinction to actually prove the create-time value, not the separate exec helper's own unrelated one, see the design note for why) and `ocibox create`/`ephemeral --platform` (`docs/design/0403`), matching real `distrobox create --platform` exactly -- reuses the exact same `os/arch[/variant]` parsing `ociman pull`/`run`/`create --platform` (0307) already established, moved out of `ociman`-private code into the shared `oci_spec_types::image` (where `Platform` itself already lives) the moment this second, genuinely unrelated caller needed the identical real parsing -- a verified-zero-behavior-change move, `ociman`'s own three existing call sites needed no changes of their own at all and `ocibox rm --rm-home` (`docs/design/0405`) -- accepted for real CLI compatibility with real `distrobox rm --rm-home`, but a genuine, checked-directly no-op: reading `~/git/distrobox/pkg/commands/rm.go`'s own actual implementation (not just its help text) revealed real distrobox's own `--rm-home` only ever removes a box's own custom home after a real interactive confirmation prompt this project has no equivalent of at all, so under the one real, always-non-interactive mode `ocibox` can ever run in, real distrobox's own `--rm-home` never actually removes anything either -- a genuinely faithful port is this exact same no-op, not the unconditional removal a surface reading of its own name alone would have suggested, verified end to end against a real custom `--home` directory that survives `rm --rm-home` completely untouched |
| 8 | packaging (rpm/deb), docs polish, release workflow | in progress — RPM packaging's own first slice (0216): a real spec (`packaging/rpm/oci-tools.spec`) building all six binaries from source (the exact same `cargo build --release --locked --offline` every other CI check already uses) and installing them at real, correct paths (`ociboot-init` to `/usr/libexec/oci-tools/`, not `/usr/bin/`, matching its own "picked up by a future dracut module, never invoked directly" role), verified locally via `ci/build-rpm.sh` (a real, from-scratch `rpmbuild` run against a real `git archive` source tarball, extracting the result and running every CLI binary's own `--version` as a real smoke test) — not yet wired into CI (this project's own current runners are Ubuntu/dpkg, and a real RPM-vs-dpkg package-database mismatch, checked directly, means neither `BuildRequires` checking nor a real `rpm -i` install verification means anything meaningful there yet; a real RPM-native CI job needs this project's own existing CentOS Stream 10 VM harness instead, still ahead). DEB packaging's own second slice (0217): `packaging/deb/control`/`packaging/deb/copyright`, same six binaries/paths, `Depends:` computed for real via `dpkg-shlibdeps` against the actual staged binaries (never hand-maintained), verified via `ci/build-deb.sh` — since this project's own dev environment genuinely is a dpkg-native host, this goes one step further than the RPM slice with a real `sudo dpkg -i` / `--version` / `sudo dpkg -r` round trip, confirmed to leave the host exactly as it started. DEB packaging is now wired into `.github/workflows/ci.yml`'s own `native-test` job (0218): that runner is genuinely dpkg-native (`ubuntu-24.04-arm`), so the real `sudo dpkg -i`/`--version`/`sudo dpkg -r` round trip now runs on every push and pull request, no new build-dependency package needed (`dpkg-dev` is already a transitive dependency of `build-essential`, which `ci/vm-prepare.sh` already installs). RPM's own real correctness is now confirmed *and* wired into CI (0224-0227): a manual, hands-on CentOS Stream 10 VM verification first found and fixed two real, distro-specific blockers neither the Ubuntu dev host could ever have surfaced (`protoc` not dnf-installable at all, fixed by vendoring it rather than requiring a host package — `docs/design/0224`; a real RPM-native distro's own default debug-info subpackage generation failing outright for Rust's own DWARF shape, fixed with `%global debug_package %{nil}` — `docs/design/0225`), then the `vm-test` matrix's own `centos-stream10` cell was taught to run this same real `rpm -i`/`--version`/`rpm -e` round trip for real, on every push and pull request, via a new opt-in `OCI_RPM_VERIFY_INSTALL=1` flag with its own automatic `rpm -q rpm` safety guard so it can never fire on a non-RPM-native host by mistake (`docs/design/0227`) — the built RPM is now a real CI artifact too. systemd units, dracut/initramfs-tools integration, and a real release workflow are all still ahead too (see `docs/design/0216`-`0218`, `0224`-`0227`) |

## Layout

```
crates/   shared libraries (all real logic lives here)
bin/      the six binaries (thin frontends)
tests/    cross-binary integration tests
ci/       reusable VM harness + repo guards (used by GitHub Actions)
docs/     architecture + design notes
dracut/   90ociboot dracut module            (milestone 5)
examples/ bootable OS Containerfiles          (milestone 4/5)
packaging/ rpm (CentOS Stream 10) + deb (Ubuntu 26.04)   (milestone 8)
```

## Building

Needs the pinned stable toolchain from `rust-toolchain.toml` (rustup picks it
up automatically):

```sh
cargo build --workspace
cargo test  --workspace
```

Lints as CI runs them:

```sh
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
python3 ci/guards.py
```

See [docs/HACKING.md](docs/HACKING.md) for the CI VM harness and development
workflow.

## License

Apache-2.0, see [LICENSE](LICENSE).
